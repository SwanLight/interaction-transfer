# 进度

> 本文件已按 2026-08-27 的修订同步（`decisions.md` D-10 ~ D-19）。旧版的 `R0/R3` 命名、32-patch 分辨率、25pt 硬阈值均已作废。

## 环境（2026-08-27 实测，中途不升级）

服务器 `root@10.0.6.98`（LXC 容器，PID1 = supervisord，主机名 `kaic-bb9c28fb-…-0`）。免密登录已配好（本地 `~/.ssh/id_rsa`，需 `ssh-add --apple-use-keychain`）。

| 项 | 值 |
|---|---|
| OS / 内核 | Ubuntu 24.04.2 LTS / 5.15.0-60-generic |
| **Isaac Sim** | **5.1.0-rc.19+release.26219.9c81211b.gl** ⚠️ 是 RC 不是正式版 |
| **Isaac Lab** | **2.3.1**（isaaclab 0.48.0 / tasks 0.11.6 / assets 0.2.3 / rl 0.4.4 / mimic 1.0.15）位于 `/workspace/isaaclab`，**editable 安装** |
| Isaac Lab commit | ❌ **无 .git，无法记录 commit**，见下方风险 R-2 |
| Python | 3.11.13（`/isaac-sim/kit/python/bin/python3`，经 `/isaac-sim/python.sh` 调用）；系统 python 是 3.12.3，**不要用它** |
| conda | 无 |
| PyTorch | 2.7.0+cu128 |
| RL 框架 | **rsl-rl-lib 3.0.1** ✅、rl_games 1.6.1、skrl 1.4.3、stable_baselines3 2.7.0 |
| GPU | 8 × RTX 4090（24564 MiB each），驱动 550.54.14 |
| CPU / 内存 | 128 核 Xeon Platinum 8358P @2.60GHz / 376 GB |
| 磁盘 | `/` overlay 14T，已用 907G，**可用 13T** |
| ffmpeg | 6.1.1-3ubuntu5 ✅ |
| nvcc | ❌ 未安装（torch 自带 runtime，仅在需要编译自定义 CUDA kernel 时才是问题） |
| 外网 | ✅ S3 资产源和 pypi 均可达 |
| **Isaac Lab 快照 commit** | **`2ab57ade`**（= upstream `v2.3.1 / 5c2ec81` + `assets.py` 补丁），见 D-22 |
| 项目 git commit（起点） | `2d454a2`（首次提交，2026-08-28） |

### 冒烟测试

`/isaac-sim/python.sh /tmp/isaac_smoke.py` → **EXIT=0**，16.5 s 完成启动→关闭。GPU/驱动/渲染栈正常。

headless 下 `GLFW initialization failed` / `failed to open the default display` / `carb.audio eDeviceLost` 是**正常噪音**，可忽略。

---

## ⚠️ 开工前必须处理的风险

### ✅ R-1 已解决（2026-08-27）

`/mnt/isaacsim_assets` 已填充：**308.1 MB / 297 文件 / 0 失败**。下载脚本留在服务器 `/tmp/fetch_assets.py`，进代码库时移到 `tools/fetch_assets.py`。

已拉取的前缀：

```
Isaac/Robots/WonikRobotics/AllegroHand/     15 文件    4.8 MB
Isaac/Robots/FrankaRobotics/FrankaPanda/    75 文件   37.3 MB
Isaac/IsaacLab/Robots/FrankaEmika/          57 文件   10.4 MB
Isaac/Props/Sektion_Cabinet/                18 文件    0.4 MB
Isaac/Props/Mounts/SeattleLabTable/         32 文件   73.3 MB
Isaac/Props/Blocks/                        100 文件  181.8 MB
```

只拉了用得到的（整树 >26.8 GB）。因为 D-07 规定新物件全用参数化 primitive，真正需要的内置资产就这几个。缺了再补拉。

### ✅ R-4 已解决（2026-08-27）

接触传感器在 Isaac Lab 2.3.1 上**完全可用**，但需要绕过包装层。三轮验证脚本：`/tmp/verify_contact.py`（v1）、`/tmp/verify2.py`（v2，定位根因）、`/tmp/verify3.py`（v3，最终确认）。

**最终结论**（v3，mass=0.5 kg，μ=0.4，μmg=1.962 N）：

| 施加力 | 摩擦力(单 env) | 速度 | 模式 |
|---:|---:|---:|---|
| 1.0 N | 1.000 N | ~0 | STICK |
| 1.5 N | 1.500 N | ~0 | STICK |
| 2.5 N | **1.962 N** | 0.54 m/s | SLIDE |
| 3.0 N | **1.962 N** | 1.04 m/s | SLIDE |

`counts=[4,4]`、`contact_pos_w` 无 NaN、`force_matrix_w=[0,0,4.905]`、points/normals 全部正常。**`plan/02` 的 Contact Mode 和 Interaction Region 两个字段均可实现。**

三个前提条件（缺一不可，已写进 `plan/01` §1 规则 7、8）：

1. filter 目标必须是 **rigid body**（不动的用 `kinematic_enabled=True`），静态碰撞体不行 → P-17
2. `filter_prim_paths_expr` 非空 + `max_contact_data_count_per_prim ≥ 1`
3. 逐点 buffer 按 env 打包，必须用 `counts`/`start_idx` 切片 → P-18

摩擦力走 `contact_physx_view.get_friction_data(dt)`，不升级 Isaac Lab，理由见 `decisions.md` **D-20**。

### ~~R-1 · 资产目录是空的~~（已解决，保留原始记录）

`/workspace/isaaclab/source/isaaclab/isaaclab/utils/assets.py:27` 把资产根**硬编码**为本地路径：

```python
# NUCLEUS_ASSET_ROOT_DIR = carb.settings.get_settings().get("/persistent/isaac/asset_root/cloud")
NUCLEUS_ASSET_ROOT_DIR = "/mnt/isaacsim_assets/Assets/Isaac/5.1"
```

而 `/mnt/isaacsim_assets` **是空的**。所有 `ISAAC_NUCLEUS_DIR` 引用都会解析失败。

云端 5.1 资产树存在且可达，本计划需要的三个资产已实测 200：

| 资产 | 路径 |
|---|---|
| Allegro | `Isaac/Robots/WonikRobotics/AllegroHand/allegro_hand_instanceable.usd` |
| Franka | `Isaac/Robots/FrankaRobotics/FrankaPanda/franka.usd` |
| Sektion 柜 | `Isaac/Props/Sektion_Cabinet/sektion_cabinet_instanceable.usd` |

整树 >26.8 GB / >40k 文件（S3 分页统计到 40 页仍未结束）。磁盘 13T 完全够。

**处理方案未定，见下方"待决"。**

### ✅ R-2 已解决（2026-08-27）

对 `/workspace/isaaclab` **原地** `git init` 做快照，不重新 clone、不改动任何文件。见 `decisions.md` **D-22**。

```
本地快照   2ab57ade5f9647e22503ce676553b6941187467d
upstream   v2.3.1 = 5c2ec81   (remote 已加，可随时 diff)
```

与 upstream 的实质差异**只有一处**——`source/isaaclab/isaaclab/utils/assets.py` (+3 -1)，即资产根路径补丁。其余 1155 个"改动"文件全是发行包裁掉的 `.github/` CI 配置、`.vscode` 模板和 `isaaclab.bat` 换行符差异。**确认没有其他隐藏的本地修改。**

复查命令：

```bash
ssh root@10.0.6.98 'cd /workspace/isaaclab && git diff --numstat upstream-v2.3.1 HEAD | awk "\$1+\$2>0"'
```

### ~~R-2 · Isaac Lab 不是 git 仓库~~（已解决，保留原始记录）

### R-3 · Isaac Sim 是 RC 版本（5.1.0-**rc.19**）

不是正式发布版。RC 可能有已知未修的 bug，且未来若被替换成正式版，行为可能变化。至少要把当前版本号锁死并在论文中如实写明。

### ~~R-4 · pitfalls P-01 ~ P-08 是针对旧版 Isaac Lab 写的~~（已解决，见上方）

复核结论已写进 `pitfalls.md` 顶部对照表 + 新增 P-16/P-17/P-18。P-06 ~ P-08（录像相关）仍未复核，留到 S0。

### R-5 · 待办：把验证脚本收进代码库

以下脚本目前只在服务器 `/tmp` 下，重启即丢：

| 服务器路径 | 应移到 |
|---|---|
| `/tmp/fetch_assets.py` | `tools/fetch_assets.py` |
| `/tmp/verify3.py` | `tools/check_contact_sensor.py` |

另需新写一个逐点接触数据的切片工具（约 30 行，按 `counts`/`start_idx` 拆 env），产出 `plan/02` §3.2 需要的格式。见 P-18。

---

## 步骤状态

| 步骤 | 内容 | 状态 | 完成日期 |
|---|---|---|---|
| S0 | 可视化链路 | ✅ 通过 | 2026-08-28 |
| S1 | 资产自检 | ✅ 通过 | 2026-08-28 |
| S2 | 脚本可行性 + Expert（按需） | 🟡 抽屉×钩杆 ✅ | 2026-08-28 |
| S3 | Source 脚本 + 数据集 + 探针物体集 | ✅ 抽屉/旋钮/擦拭/探针集 15/15 | 2026-08-29 |
| S4 | Interaction 提取 + 泄漏检查 | ✅ 通过 | 2026-08-30 |
| S4.6 | 传感可观测性（实验七） | ✅ 通过 | 2026-08-30 |
| S4.5 | 分辨率敏感度扫描 | ⛔ **被 S6 卡住**（要训好的执行器） | |
| S5 | Shared Structure Model | 🟡 全量 artifact + 九项闸门已跑，见 `../out/s5/` | conformal region 标定通过（抽屉）；mechanics 带形式待定；擦拭 §7-8 因 D-69 不可评估 |
| S6 | **E-I 交互跟踪执行器** | ⬜ 未开始 | |
| S7 | **E-I 留出任务零样本（主闸门）** | ⬜ 未开始 | |
| S8 | E-T 信息条件对照 C0–C5 | ⬜ 未开始 | |
| S9 | 扰动恢复与鲁棒性 | ⬜ 未开始 | |
| S10 | Cross-embodiment + 反事实 + 报告 | ⬜ 未开始 | |

状态图例：⬜ 未开始 / 🟡 进行中 / ✅ 通过 / ❌ 受阻

**任务推进顺序：抽屉（打通管线）→ 擦拭（主任务）→ 旋钮（支撑）。** 不要在抽屉上做第一个表示实验——它是预期阴性对照。

---

## S0 — 可视化链路 ✅ 通过（2026-08-28）

判据见 `../plan/06-eval-and-visualization.md` §1 §3。

- [x] headless 渲染可用（**需 `AppLauncher(enable_cameras=True)`**，不是裸 SimulationApp）
- [x] 能录 mp4（6 个场景，`tools/s0_record.py` + `tools/s0_all.sh`）
- [x] `report.html` 可生成（`tools/make_report.py`）
- [x] ~~WebRTC 串流~~ —— **已取消**（D-28）：服务器与开发机不同网络；且 `plan/06` §7 要的是回看归档，不是实时监看

重新生成：

```bash
./tools/sync.sh && ./tools/run_remote.sh "bash tools/s0_all.sh /tmp/s0" s0
scp root@10.0.6.98:'/tmp/s0/*.mp4' out/s1_assets/
python3 tools/make_report.py out/s1_assets log/s1/s1_report.txt out/report.html
```

⚠️ 第一个参数是**录像所在目录**（`out/s1_assets`，里面是 `<场景>_web.mp4`），
不是 `out/s0`——传错了不报错，只是六段录像全部变成"（视频缺失）"，
报告照样生成。这与 P-55 是同一类错：**没有判据的产物，出错时看起来正常**。

### 录像结果（数字与 S1 自检一致）

| 场景 | 实测 |
|---|---|
| **旋钮·轮缘**（蓝，μ=0.10） | Δθ = **+0.025 rad** —— 推不动 |
| **旋钮·销钉**（橙，μ=0.80） | Δθ = **+1.516 rad** —— 转进目标区间 1.0–2.2 |
| 擦拭·垫头杆直擦 | Fn = 5.456 ± 0.369 N，\|v_xy\| = 0.0498 m/s |
| 抽屉·全行程 | 拉开 180.0 mm，推回 0.0 mm |
| 钩杆·绕轴扫掠 | τ_hook = 0.515 N·m > τ_need = 0.420 |
| 滑块·预训练集 | 15 N 推满 150 mm |

前两行是 D-14 的**可视化对照**：同一份物理、同样 25 N 上限，只改接触区域。

### 三条本环境特有的渲染约束（P-24）

1. 必须用 `AppLauncher(headless=True, enable_cameras=True)`。裸 `SimulationApp({"enable_cameras": True})` **无效**——Camera 检查的是 carb 键 `/isaaclab/cameras_enabled`，只有 AppLauncher 会写，它还负责选 `headless.rendering.kit`。
2. `sim.step(render=True)` 即使开了 cameras **仍然卡死**。物理走 `step(render=False)`，出图时单独调 `sim.render()` + `cam.update()`。
3. `cam.set_world_poses_from_view()` **会卡死**。相机位姿必须在 `CameraCfg.OffsetCfg` 里静态给定（`tools/s0_record.py::look_at_quat`）。

### ⚠ 开环转角对推力非单调（P-26）

用户质疑「是不是阻力太大」后扫了一遍推力，结果非单调：

| 法向力 | Δθ |
|---:|---:|
| 10 N | +0.234 rad |
| 15 N | +0.392 rad |
| 20 N | **+0.217 rad** |
| 25 N | +1.516 rad |

结果不由「力够不够」决定，而由**接触维持多久**决定——开环直线推时力越大推子加速越快、越早脱离销钉。**S0 录像里的转角是示意，不能引用。**

不影响 S1 结论：D-14 的判据是接触期间传递到轴的**力矩**（τ_rim=0.170 / τ_need=0.420 / τ_pin=23.5），在接触瞬间测量，与接触持续多久无关。也不是任务设计问题——RL 学出的策略闭环、会维持接触。

我最初的解析估算（τ_need/r = 8.1 N）是**错的**，实测 10 N 远远不够。**质疑值得认真扫一遍，不能拿解析式糊过去。**

### 录像本身暴露的三个问题（都是数字发现不了的）

1. **推子飞出画面** —— 数字仍报"轮缘推不动"（正确），但视频里看起来像"推子飞了所以没转"，说服力全无。
2. **两种情况不能用同一条轨迹** —— 曾把销钉也写成"径向压紧+沿圆周走"，力穿过转轴，力矩恒为零，Δθ=0.0000。视频看起来像销钉也推不动，**完全误导**。
3. **全白渲染看不出功能区域** —— 只设了物理材质没设视觉材质，轮缘和销钉一个颜色，而它们的区别正是 D-14 的全部内容。现已配色：轮缘蓝、销钉橙。

## S1 — 资产自检 ✅ 通过（2026-08-28）

判据见 `../plan/01-assets-and-scenes.md` §7 §8。完整报告存档于 `s1/s1_report.txt`。

**51 项：49 PASS + 2 INFO，0 FAIL。** 复跑：
（抽屉 5 / 旋钮 9 / 擦拭 5 / 钩杆 4 / Allegro 7 / 双板 2 / 探针集 19；
初版只有 35 项，探针集那 19 项是 D-41 换了物体集依据之后加的。）

```bash
./tools/sync.sh && ./tools/run_remote.sh "bash tools/s1_all.sh /tmp/s1" s1
```

### 资产

六个 USD 由 `src/it/build_assets.py` 从代码参数化生成（D-07），不手工编辑、不导 URDF：

| 资产 | 自检 | 关键实测值 |
|---|---|---|
| 旋钮 | ✅ 9/9 | 关节行程 [-0.175, 3.491] rad（span 3.665，覆盖目标 1.0–2.2） |
| 抽屉柜 | ✅ 5/5 | 行程 0–180 mm，把手净空 45 mm，可用接触段 109 mm |
| 平面 + 黑板擦 | ✅ 5/5 | 见擦拭 |
| 采集板 | ✅ 2/2 | 稳态接触力 / mg = **1.0000** |
| 钩杆 | ✅ 4/4 | PD 摆位残差 0.6 mm |
| 垫头杆 | ✅ | 见擦拭 |
| 滑块（预训练集） | ✅ 3/3 | 行程 150 mm，15 N 推满行程 |
| Allegro | ✅ 5/5 + 2 INFO | 16 关节 / 21 刚体 |

### ⭐ D-14 轮缘摩擦标定通过（旋钮 region 可检验性的前提）

**实测**（非解析估算），法向力限制在安全上限 25 N 内：

```
τ_rim = 0.1697 N·m   <   τ_need = 0.420 N·m   <   τ_pin = 23.48 N·m
接触点 [4, 3]，峰值法向力 [28.15, 18.26] N
```

设计裕量（解析参考）：τ_rim=0.175 / τ_need=0.420 / τ_pin=1.300，rim 比 0.42、pin 比 3.10。

**结论：纯轮缘接触在安全力上限内传不出所需力矩，销钉可以。** `plan/05` 的 Tier 2 旋钮实验可以按原设计进行，D-14 成立。

⚠️ **数值口径注意**：τ_pin 实测 23.48 vs 解析 1.30，差一个量级。实测是**接触瞬间的峰值**，包含求解器瞬态；解析值才是设计参考。两者的**定性排序一致且裕量充足**，这是判据所依赖的。若将来要在论文里引用具体数值，应改用稳态窗口重测。

### Allegro 自动测量（`plan/01` §2 要求以实测为准）

```
整手 AABB      188.9 × 168.4 × 281.5 mm
指尖最大间距    342.6 mm
指尖 body      index/middle/ring/thumb_link_3 + middle/ring/thumb_biotac_tip
```

跨抽屉把手净空 45 mm ✓、对捏销钉 20 mm ✓。**按 D-11 未对物体做任何缩放。**

### 擦拭（主任务）

```
filter 通道有效     接触对 counts=[4,4]
法向力              5.079 N（目标区间 3–8）
contact_pos_w      NaN 比例 0.000
平稳滑移            |v_xy|=0.0498 m/s（指令 0.05），v_z=-0.0108
面接触稳定性        Fn = 5.456 ± 0.369 N（变异 6.8%），最小 5.086 N
```

### 钩杆

```
PD 摆位残差   0.6 mm
接触          扫掠过程峰值 3 个接触点
传递力矩      τ_hook = 0.5152 N·m > τ_need = 0.420（峰值法向力 10.11 N）
转矩方向      Δθ = +0.1153 rad（方向正确）
```

判据是**能否传递足够力矩**（几何可行性），不是开环扫掠脚本能转多少度——后者是控制问题，属 S2 Expert 范畴。

### S1 期间发现并固化的规则

`plan/01` §1 新增规则 7–11，`pitfalls.md` 新增 P-16 ~ P-22，`decisions.md` 新增 D-24 ~ D-27。
其中三条会**静默**毁掉实验、不报任何错：

1. 摩擦组合模式默认 `average`（规则 9）→ D-14 失效
2. filter 目标非刚体（规则 7）→ region 和 mode 两个字段作废
3. `set_joint_effort_target` 残留（P-21）→ 物体一直被驱动，几何全错

## S2 — 场景组装 + Privileged Expert 🟡 进行中

⚠️ **Gate A 已改为两层，见 `decisions.md` D-30。** 强制的是脚本可行性验证，
不是 9 个 Expert 全训。硬规则：**没为某组合训过 Expert 之前，
不许把它的失败归因于「表示信息不够」**。

### 已完成

- 抽屉环境 `src/it/envs/drawer.py`：50 Hz 控制（物理 150 Hz × decimation 3），
  39 维特权观测，无 NaN，reward 有梯度
- PPO 管线 `tools/s2_train.py`：rsl-rl 3.0.1，需要新 API 的 `obs_groups` 键
- **脚本可行性验证 `tools/s2_scripted.py`**（开训前必跑）

### 抽屉 × 钩杆的可行性验证结果

```
钩杆能拉开抽屉                100%（4/4 env），最大开度 180 mm
动作空间够用                  0.0% 的步触到单步幅度上限
reward 标定（成功轨迹上）      progress +27 / success +90 / reach +2.8 / 合计 +120
```

**接近轨迹的几何要点**（这个花了几轮才对）：

```
净空 x ∈ [18, 63] mm，中心 40.5 mm
主杆直径 16 mm -> 两侧各余 14.5 mm
支撑柱在 y = ±62.5 mm，y=0 这一列是空的，竖直主杆可自由下探
横钩若指 +X，下探时会扫过把手所在 x 区间而撞上
-> 绕 Z 转 90° 让横钩指 +Y，竖直下探进净空，再沿 +X 拉
   （拉抽屉靠主杆前面压把手背面，横钩只需不碰撞）
```

### 抽屉 × 钩杆 Expert（S2 到此为止）

⚠️ **本段的早期版本报过「固定/随机均 100%」，那个结果已作废** ——
它跑在抽屉阻尼 3.0 上，策略学会了「捅一下让抽屉自己滑」（P-28）。
下面是修正物理后的真实数据。

`expert_drawer_hook_v2 / model_100.pt`，各 256+ 条 episode：

| 环境 | 成功率 | 飞出边界 | 超时 | 终止开度(中位) |
|---|---:|---:|---:|---:|
| 固定 | **43.1%** | 0% | 56.9% | 116.6 mm |
| 随机 | **49.3%** | 0% | 50.7% | 120.9 mm |

失败全部是超时，**零次飞出边界**——策略不会把事情弄坏，只是不够快。

**Gate A 通过**（D-32 新门槛「能明确学会」）。停训于第 171 轮，
reward 70 仍在上升——诊断目的已达到，继续磕一个不进入最终系统的东西没有意义。

### 接触持续性（决定性检查，`plan/06` §7）

| | 上一版（阻尼 3） | 本版（阻尼 30） |
|---|---:|---:|
| 开启过程中**无接触**占比 | **60%** | **22.8%** |
| 受力方向正确（+X 推开） | — | **70%** |
| 运动期间平均接触力 | — | 12.7 N |

录像见 `out/s2_expert/`：钩杆的 L 形弯头朝向把手，抽屉被**逐步**拉开。
对比上一版——那时钩杆撞一下就弹开，抽屉靠惯性滑完剩下的行程。

### 为什么成功率从 100% 掉到 43%

**因为 100% 是错的。** 抽屉阻尼 3.0 时滑行距离 `v·m/c = 80 mm`，
而总行程才 180 mm，一次冲量能滑完大半。阻尼提到 30 后滑行降到 8 mm，
**任务真正变难了**。43% 是真实难度下的成绩，100% 不是。

顺带：这个修复对主线是必需的——`plan/02` §3.5 要把接触力记进
Oracle Interaction Record，而「捅一下滑行」采出来的接触数据是垃圾。

### 钩杆的已知特性：主杆承担接触，横钩几乎不参与（D-34）

实测 1423 个接触点：

- **钩杆侧**：横钩（真正的钩子）只占 **4.3%**，主杆占 **78%**
- **把手侧**：柱外伸出段（仅 15 mm 长）占 **37.6%**，中央段（108 mm）只占 16.6%；
  另有 33.9% 的接触压根不在把手上，是杆子蹭面板

**几何原因**：主杆 250 mm vs 横钩 50 mm，半径相同——主杆的可接触面积压倒性大，
且竖直插入时总有一段落在把手高度。策略用主杆是几何决定的，不是"偷懒"。

本轮**不改**，记为已知特性。理由与后续影响见 D-34。

### 遗留：接触力仍有罕见尖峰

训练中 `diag/contact_force_max_N` 峰值约 950 N（2048 env 的最大值），
但**均值只有 11–15 N、平均超出上限 0.78 N**，力惩罚占比 -0.039/步，
不影响训练收敛。

**S4 记录 Oracle Interaction Record 时必须加力值离群过滤**——
`plan/02` §8 已有「异常帧必须剔除」的条款，按力的量级过滤即可覆盖。

### 待办

1. 等 Expert 跑完 → 看成功率 → 判断 Gate A 的 95%/85% 现实不现实
2. 按 `../plan/01` §0 顺序推进：抽屉 → **擦拭（主任务）** → 旋钮
3. 每个新组合**必须先跑 `tools/s2_scripted.py`**，确认可行 + 标定 reward，再开训

## S3 — Source 数据集 ✅（抽屉 / 旋钮 / 擦拭 / 探针集全部完成）

判据见 `../plan/03-source-and-dataset.md` §4、§6、§7。

| 项 | 目标 | 实际 |
|---|---|---|
| **抽屉成功轨迹** | **≥450（≥3 策略家族）** | ✅ **739 条 / 5 家族**（共 800，成功率 92.4%） |
| **旋钮成功轨迹** | **≥1000（≥4 策略家族）** | ✅ **1693 条 / 4 家族各 ≥286**（共 2400，70.5%；`rim_only` 480 条按设计失败，D-46） |
| **擦拭成功轨迹** | **≥1250（≥5 家族，含直擦）** | ✅ **2606 条 / 6 家族全部 ≥370**（共 2700，96.5%） |
| **探针物体集** | 每原语 ≥200 且 ≥2 个承载物体（D-41） | ✅ **10740 条 / 9250 成功**，15 条原语 **15/15** 两条判据都满足（D-47） |
| 一致性校验标记率 | <15% | ✅ 0.00%（脏帧比例） |
| 人工看过的轨迹视频 | ≥10 条 | ✅ 抽屉/擦拭/旋钮各 5~6 个家族的录像 + 逐帧接触轨迹核对 |

- [x] 校准集已独立划出（D-17）——55 条
- [x] 划分按 **episode** 而非帧（`pitfalls.md` P-10）
- [x] 策略分类器在**原始 source 动作**上准确率显著高于随机（多样性验收）——
      抽屉 0.742 / 随机 0.200（3.7×），擦拭 0.987 / 随机 0.167（5.9×），
      旋钮 1.000 / 随机 0.250（4.0×），`tools/s3_diversity.py`

### 抽屉 × 双板：800 条，739 成功

```bash
./tools/sync.sh
./tools/run_remote.sh "PYTHONPATH=src /isaac-sim/python.sh \
    tools/s3_source_drawer.py --envs 80 --batches 10 --out /tmp/s3_drawer_v3" s3full
ssh root@10.0.6.98 'cd /workspace/interaction_transfer && \
    PYTHONPATH=src /isaac-sim/python.sh tools/s3_verify_dataset.py /tmp/s3_drawer_v3'
```

数据集在服务器 `/tmp/s3_drawer_v3`（36 MB，800 个 npz + manifest），
按 D-23 不进版本控制。产物摘要在 `out/s3_source/`。

⚠️ **本节的数已换成 `_v3` 那一版**（下方表格与 `out/s3_source/report.txt` 一致）。
初版写的是更早的 `/tmp/s3_drawer_full`——它**缺 `rel_vel_obj`、几何/跨实现划分全空**，
成功 737 条、杆背面 90.22%。那一版已作废，S4 用的是 `_v3`。

| 家族 | 条数 | 成功 | 开度 mm | 操作阶段脱手 | 受力方向对 | 接触在杆背面 |
|---|---:|---:|---:|---:|---:|---:|
| `pinch_center` 中央夹持 | 160 | 122 | 151.8 | 6.0% | 90.4% | 73.3% |
| `pinch_offset` 偏置夹持 | 160 | 140 | 149.2 | 6.6% | 93.5% | 78.5% |
| `hook_both` 双指纯勾 | 160 | 157 | 139.6 | 12.0% | 100.0% | 100.0% |
| `asym_primary` 非对称主副 | 160 | 160 | 141.1 | 10.2% | 100.0% | 100.0% |
| `single_finger` 单指 | 160 | 160 | 148.9 | 6.7% | 100.0% | 99.9% |

pinch 两族的"杆背面"只有 73~78%，**剩下的都在杆正面**——那正是拇指该在的地方，
不是异常。

### ⭐ 接触部位：这一段是本步的验收核心

D-34 的教训是"成功率正常但接触落在完全出乎意料的地方，而数字看不出来"。
所以每条 episode 都带部位归类（`src/it/contact_attrib.py`），800 条汇总：

| 抽屉上的部位 | 占比 | | 板上的面 | 占比 |
|---|---:|---|---|---:|
| **把手横杆背面** | **90.33%** | | **工作面** | **100.00%** |
| 把手横杆正面（拇指） | 9.63% | | 背面 | 0.00% |
| 横杆上/下表面 | 0.00% | | 侧边 | 0.00% |
| 支撑柱 / 面板 / 其他 | 0.04% | | | |

另有 **100% 的接触落在两根支撑柱之间**的横杆自由段——
不是 D-34 里钩杆那种"挂在柱外伸出的边角上"。

其他健康指标：峰值单点接触力 **11.55 N**（安全上限 25 N）、脏帧 0.00%、
PD 饱和 0.00%、板姿态误差均值 5.6°/峰值 12.0°、
非过滤接触力/过滤接触力 = 0.42（>1 说明碰到了抽屉以外的东西，这里没有）。

接触模式（`plan/02` §3.4，按法向力加权）：
sticking 35.5% / sliding 35.2% / **separating 29.3%**。

⚠️ **separating 占到 29% 是一个要带进 S4 的观察**，不是错误：
PhysX 只在部分物理子步里报告某个接触对（P-31），被采到的那一子步常常
正处在"斑块刚要分开"的状态。几何上接触是连续的（板与横杆的间隙全程
稳定在 −0.5 mm），但**报告是断续的**。S4 构造 Oracle Interaction Record 时
要决定这一档怎么处理——直接当成"分离"会低估接触的持续性。

### 独立验收（`tools/s3_verify_dataset.py`，18 项全通过）

不看生成脚本自己报的数，从落盘文件重读一遍：划分不重不漏、
`unseen_strategy_test` 只含留出家族且该家族不在训练集、
`unseen_physics_test` 的阻尼取值无一落在训练区间 [24.2, 36.0] 内、
抽查 60 条 SHA-256 一致、`model_arrays()` 里没有 `source/*` 审计字段、无 NaN。

划分（`_v3`，与 `out/s3_source/verify.txt` 一致）：train 323 / calibration 43 /
in_distribution_test 64 / unseen_geometry_test 83 / unseen_physics_test 66 /
unseen_strategy_test 160（留出家族 `asym_primary`）/ failed 61。
⚠️ 留出的物理参数取值 [16.01, 44.99] **无一落在训练区间 [24.01, 35.99] 内**——
这一条是 `plan/03` §7 的硬要求，不是统计描述。

### 录像验收（`out/s3_source/`）

五个家族各一条 mp4 + 六格接触表。**板上加了纯视觉朝向标记**
（工作面浅色贴片 + 顶边深色鳍，D-35），否则薄板前后对称，
录像里根本看不出哪一面在接触——`plan/06` §7 的人工检查会形同虚设。

逐帧核对过：板与横杆的间隙全程稳定在 −0.5 mm（实实在在压着），
抽屉被匀速拉开，不是 P-28 那种"捅一下让它自己滑"。

### 这一步踩的七个坑

`pitfalls.md` **P-30 ~ P-36**。其中 P-30 最重要，也推翻了 P-18 的既有做法：
逐点接触数据的 **env 归属是错的，而且会整段丢失**。
两个静态对拍脚本全部通过（所有 env 同时接触时下标恰好恒等），
只有真实采集里接触先后建立才会错开。修复后保留的接触点从 5730 涨到 13786。

⚠️ **D-34 的钩杆接触分布是用旧方法算的，引用具体百分比前需要重算。**

**P-36 是收尾时才发现的，代价是数据集全量重采一次**：摩擦 buffer 有自己的
counts/start_idx，拿接触的下标去索引它，env0 的摩擦翻倍、env1 全是零。
`plan/02` §3.4 的 Contact Mode 直接建立在摩擦上，所以第一版的 mode 标签
整体是错的（stick_share 0.72 → 0.36）。发现它是因为**复跑了
`check_contact_sensor.py`**——而那个脚本自己也坏了（写文件不写 stdout、
结尾 `app.close()` 挂起），所以坏了很久没人发现。两条都已修。

## S3 · 探针物体集（交互原语库）✅

物体集的**选取依据**在 2026-08-29 换过一次，理由是用户指出的一个真实逻辑漏洞：
原先是照着留出任务需要什么反推物体，那样执行器在"摸索能力"阶段学到的就是
留出任务本身，只换了个标签。现在改为按一套独立于本项目三个任务的五轴框架做
**边缘覆盖**（Huang 的接触模式枚举 + Bullock 的手中心描述子 + Lynch & Mason 的
非抓握原语命名），并加一条硬规则：**每条原语至少两个几何不同的物体承载**。
五轴是项目综合，不是完备分类学，边缘覆盖不等于张满笛卡尔积（D-60）。
完整论证见 `../plan/03` §2.4，决策见 D-41 / D-43 / D-60。

```bash
# 每物体一张卡，batches 按"每条原语 ≥200 条成功轨迹"算
PYTHONPATH=src /isaac-sim/python.sh tools/s3_source_probe.py \
    --object block --envs 60 --batches 34 --out /tmp/s3_probe/block
# 十五原语与双物体覆盖核对（不声称笛卡尔积完备）
PYTHONPATH=src /isaac-sim/python.sh tools/s3_coverage.py /tmp/s3_probe
```

**10 个物体、10740 条 episode、9250 条成功（86.1%）。**

| 物体 | 条数 | 成功率 | | 物体 | 条数 | 成功率 |
|---|---:|---:|---|---|---:|---:|
| `block` | 2040 | 90.0% | | `plunger` | 840 | 73.0% |
| `column` | 1440 | 65.1% | | `dial` | 600 | 100% |
| `roller` | 1620 | 61.7% | | `flap` | 420 | 100% |
| `ball` | 840 | 83.5% | | `ridge` | 840 | 100% |
| `slider` | 1260 | 100% | | `slab` | 840 | 100% |

**15 条原语 15/15 全部满足"≥200 条成功"与"≥2 个承载物体"**，
逐条见 `out/s3_probe/coverage.txt`。上一轮是 10/15，五格是这样补上的（D-47）：

| 格 | 原来 | 现在 | 补的是什么 |
|---|---|---|---|
| P7 `roll` | 0 / 0 物体 | 399 / 2 | **把 `ball` 真正采进来**——它在采集器里早有定义，但物体集只列了九个、从没跑过；球 210/210，卧柱 189/810 |
| P8 `twist` | 191 / 1 | 271 / 2 | 球：两侧对置、切向反向搓，绕竖直轴转（71/210） |
| P3 `slide_push` | 204 / 1 | 249 / 2 | 立柱：与 push 共用低位点，推到打滑（45/205） |
| P13 `pinch_hold` | 204 / 1 | 414 / 2 | 滑轨块挡片的两个窄侧面（210/210） |
| P14 `pinch_move` | 204 / 1 | 414 / 2 | 同上，捏住再沿导轨推（210/210） |

**分类学一个格都没删**（D-41）。试过而不成立的也记下来：自由卧柱的**两端面
对捏**不可行——板还没夹上来圆柱就先滚跑了（1020 条里 pinch_hold 只成功 2 条、
pinch_move 与 twist 各 0 条，位移 215~1399 mm）。

另外补了一条判据漏洞：`expect="move"` 只查世界位移，会把"捏住滑轨块沿导轨
推了 46 mm"判成"物体没有被推动"——`plan/02` §3.1 把受约束平移 E3 与自由
平移 E1 都算作"物体被移动"，现在两者取或（与 `expect="turn"` 分支同理）。

采集器按**原语 × 接触位点**组织：物体只声明位点（局部系位置、外法向、切向、
进入方向），原语只声明贴上去之后怎么动。两者叉乘，加物体不必写新阶段机——
而且这与 `plan/02` 的规格字段（region / engage 方向 / mode / mechanics）同构。

录像与六格接触表：`out/s3_probe/`，九条原语各一条（含补上的三格）。

### 这一步踩的三个坑

`pitfalls.md` **P-37 ~ P-39**：接触法向的正负约定换个场景就翻（判"哪一面"
不能用它）、运行时写关节阻尼会盖过资产里的值（改了资产等于没改，
"结果逐位相同"是改动没生效的强信号）、力控的轴掩码是世界轴对齐的
（法向不与世界轴对齐时三轴全成力控，切向指令彻底失效）。

## S3 · 擦拭 source（主任务）✅

`plan/01` §5.1 的两种实现都做了：持工具擦（黑板擦）与自身接触面直擦。
两者产生同一份记录格式，因为 `plan/02` §1.1 把被操作物体定在**平面**上。
控制上三个轴全走位置控制、压力由位置干涉给（D-44）。

```bash
PYTHONPATH=src /isaac-sim/python.sh tools/s3_source_wipe.py \
    --envs 60 --batches 45 --out /tmp/s3_wipe
PYTHONPATH=src /isaac-sim/python.sh tools/s3_dirt_map.py /tmp/s3_wipe --out dirt.png
```

六个家族：`tool_center` / `tool_offset` / `tool_tilt` / `tool_heavy_slow` /
`tool_light_fast` / `direct_wipe`。**`direct_wipe` 是 `plan/02` §7 第 8 条
泄漏检查（envelope 与"是否用工具"无关）唯一的数据来源，不可省。**

**2700 条 / 2609 成功（96.6%），六个家族全部 ≥373 条成功轨迹。**

| 家族 | 条数 | 成功 | 清除% | 力在区间% | 力N |
|---|---:|---:|---:|---:|---:|
| `direct_wipe` | 450 | 373 | 75.1 | 82.9 | 4.73 |
| `tool_center` | 450 | 450 | 88.2 | 91.5 | 6.31 |
| `tool_heavy_slow` | 450 | 436 | 78.6 | 84.7 | 6.74 |
| `tool_light_fast` | 450 | 450 | 92.1 | 92.5 | 6.14 |
| `tool_offset` | 450 | 450 | 88.2 | 92.6 | 6.22 |
| `tool_tilt` | 450 | 450 | 88.5 | 90.5 | 6.29 |

法向力 4.7~6.7 N（允许区间 3~8），**工具滑脱 0.6~0.7 mm**（改夹长侧面之前是
1.6~13.2 mm），擦拭阶段脱手 0.0%，独立验收 21 项全通过。抓持面的改动见 D-48。dirt 覆盖图见 `out/s3_wipe/dirt_tc.png`——
录像看得到动作，**看不到污渍**，而擦拭的 effect 只有 dirt 变化，
所以这张图是擦拭验收不可替代的一半。


### S3 · 数据划分（`plan/03` §7 的一个校准集 + 五个冻结测试集）✅

| 划分 | 旋钮 | 擦拭 | 抽屉 |
|---|---:|---:|---:|
| train | 652 | 950 | 323 |
| calibration | 87 | 127 | 43 |
| in_distribution_test | 130 | 190 | 64 |
| **unseen_geometry_test** | **204** | **297** | **83** |
| unseen_physics_test | 173 | 219 | 66 |
| unseen_strategy_test | 447 | 450 | 160 |
| **unseen_implementation_test** | — | **373** | — |
| failed | 707 | 94 | 61 |

**小幅几何变化**（§7 表第 6 行）：每个任务物体多生成两个变体——销钉偏心
46/52/58 mm、把手净空 38/45/52 mm、黑板擦长 80/90/100 mm——用
`MultiUsdFileCfg` 按 env 轮转混采约 17%，单独进 `unseen_geometry_test`。

⚠️ **`replicate_physics` 必须设成 False**：为 True 时 Isaac Lab 把 env_0 的物理
整体复制给所有 env，多资产被抹平——实测 24 个 env 全部拿到名义几何，
而代码以为其中 4 个是变体，标签会照常写出来、全是假的。改完实测接触半径
47.9 / 50.6 / 59.6 mm 随三档销钉偏心清楚分开。

**跨实现留出**（§7 末尾）：擦拭把 `direct`（不用工具）那一种实现的**全部**
373 条成功 episode 留出——`plan/02` §7 第 8 条（envelope 与是否用工具无关）
将来才有干净的测试集。旋钮与抽屉各只有一种实现，该桶为空。

三份数据集独立验收：旋钮 18 项、擦拭 21 项、抽屉 18 项全 PASS，0 FAIL。

## S3 · 多样性验收 ✅

`plan/03` §4：训练一个简单策略分类器，**原始 source 动作**上的准确率
应显著高于随机。`tools/s3_diversity.py`，只用 numpy 的多分类逻辑回归，
特征取 `source/*` 的低阶统计量，按 episode 划分。

| 数据集 | 家族数 | 准确率 | 随机 | 多数类 | 提升 |
|---|---:|---:|---:|---:|---:|
| 抽屉 | 5 | **0.742** | 0.200 | 0.224 | 3.7× |
| 擦拭 | 6 | **0.987** | 0.167 | 0.173 | 5.9× |
| 旋钮 | 4 | **1.000** | 0.250 | 0.295 | 4.0× |

完整输出与三个混淆矩阵在 **`out/diversity.txt`**（2026-08-30 在
`_v3` / `_v5` / `_v4` 三份现行数据集上重跑，`./tools/run_remote.sh` 退出码 0）。

混淆矩阵是有结构的，不是噪声：抽屉里 `asym_primary` 与 `hook_both` 互相混淆
（两者站位相同、只有力分配不同，本来就该像），`pinch_center` 与 `pinch_offset`
互相混淆，而 `single_finger` 被完美分开；擦拭里 `direct_wipe` 被完美分开。

⚠️ 这一条只证明**动作层面确实不同**。`plan/02` §7 第 4 条要的是
**从 envelope 预测策略身份显著更难**，要等 S4/S5 有了 envelope 才能做，
届时两个数并排报，一高一低才说明表示抹掉了策略特异性。

### S3 · 旋钮 source ✅

**2400 条 / 1693 成功（70.5%）**，四个销钉家族分别 480 / 286 / 480 / 447 条成功，
全部达到 `plan/03` §6 的"≥4 家族、每家族 ≥250、总计 ≥1000"。
独立验收 18 项全通过，策略分类器 1.000（随机 0.250）。决策见 D-46。

```bash
IT_GPU=0 ./tools/run_remote.sh "PYTHONPATH=src /isaac-sim/python.sh \
    tools/s3_source_knob.py --envs 60 --batches 40 --video --out /tmp/s3_knob_v4" knob
PYTHONPATH=src /isaac-sim/python.sh tools/s3_knob_contact.py /tmp/s3_knob_v4
```

#### ⭐ 接触部位与受力方向（本步的验收核心）

录像看得出板在推销钉，**看不出力打在销钉的哪一侧、方向偏了多少**——
而旋钮的全部力学就在这两件事上（`tools/s3_knob_contact.py`，物体局部系加权）：

| 家族 | 成功 | 销钉% | 盘顶面% | 轮缘% | 打在**推进后侧**% | 法向偏切向° | 接触高度 mm |
|---|---:|---:|---:|---:|---:|---:|---:|
| `pin_pinch` | 480/480 | 100.0 | 0.0 | 0.2 | 100.0 | 11.7 | 26.2 |
| `pin_push_single` | 480/480 | 100.0 | 0.0 | 0.2 | 100.0 | 11.5 | 26.2 |
| `pin_push_dual` | 286/480 | 100.0 | 0.0 | 0.0 | 96.3 | 2.4 | 36.7 |
| `pin_regrasp` | 447/480 | 100.0 | 0.0 | 0.8 | 100.0 | 9.2 | 26.2 |
| `rim_only` | **0/480** | **0.0** | 0.0 | **100.0** | — | — | — |

⚠️ 销钉% 那一列的 100.0 是**四舍五入**：轮缘上还有 0.0~0.8%，所以真值是
99.2~100%。S4 从另一条路径独立算出的销钉占比 **99.83%** 与它对得上。

法向只偏离圆盘切向 2.4~11.7°，也就是说力几乎全部转成绕轴力矩。
P-42 那个错版本偏 42°，三分之一的力顶着销钉往圆心推。

其他健康指标：脏帧 0.00%、板姿态误差均值 7.0°、p95 接触力 19.2 N（上限 25）。

#### `rim_only` 是故意会失败的对照（D-14 的操作级证据）

压足 **19.5 N**（安全上限 25 N 之内）时只转到 **0.019 rad**，
而销钉家族转到 2.34~2.76 rad：

    τ_rim  = μ·F·R  = 0.10 × 19.5 × 0.070 = 0.137 N·m
    τ_need = 阻尼·ω = 0.28 × 1.5           = 0.420 N·m

S1 只做了静态力矩标定，这里是**真实操作轨迹上**的验证，
`plan/05` 实验五的反事实镜头有数据基础了。它的 episode 进 `failed` 桶。

**安全上限没有被放宽**：`MAX_PUSH_FORCE` 反而从 18 N 降到 13 N，
因为 `pin_push_dual` 两块板夹住销钉时指令力几乎全部落到物体上。上限对所有家族相同。

#### 这一步踩的六个坑

P-42 参考系挂在"领先角"而不是销钉实际位置（力大半变成径向无用功）、
P-43 单向关节却随机化转向（一半 episode 顶死挡，**改什么控制都不动**）、
P-44 光滑柱面上用纯力控（板钻到圆盘底下，1229 N 而力矩为零）、
P-45 力控从零穿透起步（板穿过销钉，**接触点数全程为 0**）、
P-46 板的长边没有与销钉轴平行（**静态位控正常、动态失效**）、
P-47 把"任务已完成之后"的窗口算进脱手率（窗口越宽裕数据越难看）。

---

## S4 — Interaction 提取 ✅（四份数据集全部提取并验收）

`plan/02` §3 的 Oracle Interaction Record。**16 640 条 episode 全部提取完成**，
schema **`s4-record-v2`**（v1 已作废、读到即报错，原因见 D-52），
划分原样继承 S3（不重划，否则校准集会被污染）。

### ⚠️ 2026-08-30 的一轮返工，接手前必须知道

外部审查 + 自查共修了六处，**其中三处会让下游结论作废而没有任何征兆**：

| # | 问题 | 后果 | 记在哪 |
|---|---|---|---|
| 1 | mode 判据初版选错（用接触斑块位移） | 抽屉 sticking 35.9% → **92.5%**，语义级错误 | D-49 修订 |
| 2 | 相对速度算在世界系、法向在物体系 | 零征兆，只有场景旋转检查抓得到 | P-53 |
| 3 | 接触体位姿世界系、接触点物体系，8~9 m 假力臂 | 擦拭与探针自由体全中 | P-54 |
| 4 | schema 未升版本 | 新旧记录 mode 语义不同却可混用 | D-52 |
| 5 | effect 各任务维度语义不同，无统一接口 | E-I 写下去必然出现任务分支 | D-53 |
| 6 | phase/progress 是任务状态机标签却当观测 | 等于给 policy 一个任务 oracle | D-55 |
| 7 | **泄漏检查脚本中途崩了，报告开头却全是 `[PASS]`** | 六份产物是废的，而我照文件头写了"七条通过" | **P-55** |

**现在提取器里有三条硬防线**：力臂 > 0.5 m 直接报错、物体世界位姿缺失直接报错、
schema v1 读到直接报错。这一类"零征兆的错"从此跑不起来，而不是靠运气发现。
**验收侧也有了一条**：全部检查由 `tools/s4_all.sh` 统一驱动，**判据是退出码**，
任一段异常都变成 `[FAIL]` 而不是把后面的检查一起带走，末尾报 PASS/FAIL/DEFER 的条数。

| 数据集 | S3 源 | S4 记录 | episode | 成功 |
|---|---|---|---:|---:|
| 抽屉 | `/tmp/s3_drawer_v3` | `/tmp/s4_drawer` | 800 | 739 |
| 擦拭 | `/tmp/s3_wipe_v5` | `/tmp/s4_wipe` | 2700 | 2606 |
| 旋钮 | `/tmp/s3_knob_v4` | `/tmp/s4_knob` | 2400 | 1693 |
| 探针集 | `/tmp/s3_probe/<10 物体>` | `/tmp/s4_probe/<10 物体>` | 10 740 | 9 250 |

⚠️ **HANDOFF 早先写的 `/tmp/s3_drawer_full` `/tmp/s3_wipe` `/tmp/s3_knob`
是上一轮的旧数据集**，缺 `rel_vel_obj`、且几何/跨实现划分全空。已更正。

### 表面采样（D-50，`src/it/surfaces.py`）

按目标 pitch 换算点数、逐部件配额、嵌套多分辨率、每份一个 sha256 写进记录 meta：

| 物体 | 点数 | 面积 | pitch | 备注 |
|---|---:|---:|---:|---|
| 抽屉（3 个几何变体） | 16 384 | 2776 cm² | 4.12 mm | 横杆背面 210 点 |
| 旋钮（3 个变体） | 8 192 | 403 cm² | 2.22 mm | |
| 擦拭平面 | 16 384 | 6440 cm² | 6.3 mm | 平面太大，是四个物体里最粗的一档 |
| 探针 10 个物体 | 4096~16 384 | | 1.9~6.1 mm | |

### ⭐ 接触部位：与 S3 独立算出来的数对拍

这是本步的验收核心。S4 走的是"归到最近表面采样点"，S3 走的是解析式部位判据
（`contact_attrib.classify_drawer_local`），**两条独立路径**：

| | S4 提取 | S3 已验收 |
|---|---|---|
| 抽屉 把手横杆背面 | **92.89%** | 90.33% |
| 抽屉 横杆正面（拇指） | 6.95% | 9.63% |
| 抽屉 支撑柱 / 面板 | 0.16% | 0.04% |
| 旋钮 销钉（柱面 + 顶面） | **99.83%** | 100% |
| 擦拭 平面工作面 | **99.68%** | —— |

### 动力学一致性（`plan/02` §8）

| 任务 | 判据 | 相关 | 幅值比 |
|---|---|---:|---:|
| **旋钮** | 重建的绕轴力矩 vs 仿真器记的 `object/axis_torque` | **0.931** | **0.90** |
| 抽屉 | 重建的沿轨力 vs `m·a + c·v` | 0.824 | 2.37 |
| 擦拭 | —— | N/A | N/A |

旋钮这一格最硬：`object/axis_torque` 是 S3 采集时另记的一路关节广义力，
与"逐接触点的力 × 力臂"是两条独立路径，对得上说明法向定向、力的作用对象、
力臂全都没错。抽屉的幅值比 2.37 是预期的——接触力里含两块板隔着把手互相顶的
**内力**与撞击瞬态（P-22），相关系数才是有意义的那个数。
擦拭平面是 kinematic 刚体，接触力不进入它的运动方程，**如实标 N/A，不拿别的数凑**。

### contact mode 重判（D-49，判据换过两次）

S3 的 separating 是采样伪影（带着力的接触被 `separation > 0` 覆盖）。
最终判据是**两刚体位姿差分**——它同时是**真实装置能测到的量**
（`plan/07` §3 的面相对物体位姿，差分即得）：

| 按法向力加权 | sticking | sliding | separating |
|---|---:|---:|---:|
| 抽屉 S3 原始 / **S4** | 32.6% / **92.5%** | 40.7% / 7.5% | 26.7% / **0%** |
| 旋钮 S3 原始 / **S4** | 42.6% / **80.8%** | 44.5% / 19.2% | 12.9% / **0%** |
| 擦拭 S3 原始 / **S4** | 14.2% / 13.1% | 44.4% / **86.9%** | 41.5% / **0%** |

夹住把手拉当然是贴着的、擦拭当然是滑的——**修订后三个任务才按物理分得开**。

### 任务无关的 effect 接口（D-53，v2 新增）

`effect/rigid` (T,H,6) 物体的刚体位移 + `effect/surface_state` (T,H,64) 表面状态变化。
关节量按关节轴换算过来，自由体直接是位姿增量，固定物体为零——
**"抽屉拉开 20 mm"与"旋钮转 0.3 rad"从此走同一条通道**，E-I 不需要任务分支。
任务原生的 `effect/current` / `effect/future` 保留但降级为 S5 与诊断专用。

### ⚠️ P-52：采集板角速度顶格，接触点相对速度整体不可信

抽屉 96.9%、旋钮 100% 的操作帧上 |ω| ≥ 99.5 rad/s（PhysX 默认上限 100）。
把那一路速度积出来是 59 mm 滑移，而接触点在物体系里一动没动。
**位置量（region / 部位 / 力）不受影响，S3 的验收结论仍然成立。**

### 其他健康指标

脏帧 0.00%；归不到表面的接触点 ≤0.1%（穿透不算，见 P-52 邻条）；
法向符号一致度 ≥0.999；总接触力超 25 N 安全上限的帧：抽屉 0.59% /
旋钮 2.61% / 擦拭 0.00% / 方块 0.14%（S3 已验收过，这里只是让它可见，
因为 C4/C5 的 mechanics 范围直接建立在这些力上）。

### `plan/02` §7 泄漏检查清单（9 条）

⚠️ **这一节 2026-08-30 晚重写过一次，原因是它此前不成立**：脚本第 2 条抛了未捕获
异常，第 3~8 条**根本没跑**，而落盘报告的开头四行全是 `[PASS]`，我照着文件头
写了"七条通过"。同一轮里三份 region 探针报告整份都是 traceback。记在 **P-55**，
检查本身的重写记在 **D-52 补记**。下面的数来自重写之后、`tools/s4_all.sh`
退出码为 0 的那次运行。

**汇总条数**（这一行本身就是判据——与"预期几条"对不上就是有检查漏跑）：
抽屉 **PASS 9 / FAIL 0 / DEFER 2**、擦拭 **PASS 10 / FAIL 0 / DEFER 3**、
旋钮 **PASS 9 / FAIL 0 / DEFER 2**。擦拭多的那一条是第 8 条（只有它有两种实现）。

- [x] 1. 场景刚体旋转后物体系表示逐元素一致 —— **代数等变性测试**：10 条 ×
      随机旋转，region 索引差 **0** 个、离阈值 5% 以外的 mode 差 **0** 个、
      effect 最大差 **0.00e+00**、滑移速度最大差 1.0~2.9e-05 m/s；
      1b 力/力矩/广义力残差 **0.00e+00**。**三份全过**——擦拭此前 DEFER，
      是因为旧数据没记平面位姿；`s3_wipe_v5` 记了（P-54 的解法），不是判据放松。
      ⚠️ 限制：只检验提取器有没有硬编码世界轴，**不检验仿真器的坐标处理**
- [x] 2a. 记录里没有 `source/*` —— 29 个数组
- [x] 2b. **删掉声明读取集以外的全部 `source/*`（指令 / 目标位姿 / 本体速度 /
      板面编号 / PhysX 世界系接触点），重跑提取，逐字段 `array_equal` 逐位相同，
      零容差**。删掉的字段数：抽屉 10、擦拭 6、旋钮 6
- [x] 2c. **反过来删掉声明要读的测量位姿，提取器必须直接报错**——宁可炸，
      也不在"物体大概在原点"之上算出一份看起来正常的记录（P-54）
- [x] 2d. 每个 `source/*` 都归成测量或动作之一，声明读取的全是测量字段
- [x] 3. 改变 source 接触体数量后表示维度不变 —— 单板 / 双板 / 三接触体
      （擦拭含工具）共用同一套字段与维度
- [ ] 4. 策略分类器难以从 envelope 识别策略 —— **只做了记录级**：从 Oracle Record
      认策略 0.900 / 0.941 / 0.742（抽屉/擦拭/旋钮），从原始 source 动作
      0.717 / 0.950 / 1.000。envelope 级留 S5
- [x] 5. target 看不到 source 身份 —— 策略家族只在 meta 的审计字段里
- [x] 6. 物理参数不直接写入表示 —— 结构性 + 可推断性探针（诊断，不设门槛）：
      从记录预测 physics_variant 0.824 / 1.000 / 0.942（均 2 类，随机 0.500）
- [x] 7. 无执行器专属 joint / contact 编号 —— 合并接触体之后就没有"第几块板"了
- [ ] 8. **擦拭 envelope 与"是否用工具"无关** —— **只做了记录级**（字段里没有工具
      的存在性/位姿/几何，两种实现记录级统计相对差中位 0.150），互换实验留 S5/S7
- [x] 9. **region 可推导性探针** —— 已做，**结论按任务不同**，且这一版的数与
      P-54 修复前那一版**完全不同**：只看 effect 预测 region 热图的可解释方差
      **抽屉 0.029 / 擦拭 0.120 / 旋钮 0.687**（家族级参照 0.151 / 0.127 / 0.553）。
      **旋钮 0.687 已经超过它自己的家族级参照（124%）**——`family` 只知道离散
      家族标签，而接触落在销钉的哪一片随转角**连续**变化，effect 给得出、
      家族标签给不出。所以**旋钮基本丧失了检验 region 必要性的能力**，
      实验一解读 C0 vs C2 时必须显式排除旋钮。擦拭 0.120 占参照的 95%，
      同样要谨慎声称。**抽屉（0.029，占 19%）是三个里唯一能干净检验 region 的**,
      region 必要性的论证要以它为主载体。按 Gate G 如实报告，不得调参掩盖。
      与 D-14 不冲突（那是力学可行性，这是信息量），两件事分开写

⚠️ 第 4、8 条**没有通过，是"本步做不完"**（要 envelope，S5 的产物），
不得当成通过。

**2b 与 2c 是一对，缺一条这条检查就没意义**：单有 2b，"一个 `source/*` 都不读"
也能通过（那正是 D-49 初版守错规矩的下场）；单有 2c，读了指令位姿也能通过。
判据不是"不读采集侧数据"，是**"只读真实装置也测得到的量，指令与动作一位都不许
影响输出"**（D-52 及其补记）。

### 怎么重跑（判据是退出码，不是报告里有没有 PASS）

```bash
./tools/sync.sh
./tools/run_remote.sh "bash tools/s4_all.sh" s4all           # 验收，约 25 min
./tools/run_remote.sh "bash tools/s4_all.sh --extract" s4all # 连提取一起
```

本机 38 条单元测试（`test_surfaces` 12 / `test_interaction` 18 / `test_records` 8）
不需要 Isaac，改完提取器立刻能验；其中 `test_action_channels_have_zero_influence`
与 `test_declared_reads_are_all_measurements` 是 2b / 2d 的本机版。

记录：`out/s4_records/`（README + 每任务 extract / verify / leak / region_probe /
sensing 报告 + region 热图 png）。

---

## S4.6 — 传感可观测性（`plan/05` 实验七，D-51 新增）✅

**为什么必须有**：idea 的核心是"不采人手动作，只采各接触面的位姿 + 面上的触觉"。
在这个实验之前，整轮计划**没有一处碰这句话**——留下的内容与 CHORD（object-centric
contact wrench）高度重叠。**属于本方案自己的那一条主张必须自己给证据。**

做法（`tools/s4_sensing.py`，纯离线、不需新训练）：拿已验收的 S4 Oracle Record
当真值，只用**装置能测到的量**（面相对物体的位姿 + 面上的 taxel）重建一遍
region / mode / mechanics，再与真值比。

| 扫的维度 | 取值 |
|---|---|
| 面相对物体位姿误差 | 0 / 0.5 / 1 / 2 / 5 mm 与 deg |
| 误差的时间结构 | 配准型偏置 / 每帧独立白噪声 |
| taxel pitch | 1 / 2 / 4 / 8 mm |
| 触觉模态 | binary / 法向 / 法向+切向 |

### ⭐ 结论：硬件指标是相对量，不是一个绝对毫米数

三个任务的功能接触区尺度差一个数量级，region 的降级速度就差一个数量级——
**5 mm 面位姿误差**下 region 余弦相似度：

| 任务 | 功能接触区尺度 | 5 mm 误差下的 region 余弦 |
|---|---|---:|
| 旋钮（销钉 Ø20） | ~20 mm | **0.158** |
| 抽屉（把手 140 mm） | ~140 mm | 0.720 |
| 擦拭（190×290 mm） | ~200 mm | **0.956** |

所以 `plan/07` §1 的面位姿精度指标应当写成"**≤ 功能特征尺寸的 ~10%**"，
而不是一个全局毫米数。**越是大面积接触的任务，对面位姿越宽容**——这对硬件
路线是实打实的利好，也说明擦拭（主任务）恰好是最容易做的那一个。

⚠️ 这三份数**曾经有一版是废的**（擦拭 0.5 mm 噪声给出 23 mm 接触点偏移，
放大 47 倍，没有物理来源）。根因是 P-54：接触体位姿在含 env 偏移的世界系、
接触点在物体系，混用出 8 m 力臂。修好后擦拭是 0.69 mm，与噪声幅值 1:1。
`lever_check.txt` 是那一类 bug 的普查：四份数据的力臂现在是
17.6 / 46.0 / 17.6 / 21.6 mm（修前擦拭 7156 mm、探针方块 9001 mm）。

记录：`out/s4_records/sensing_{drawer,wipe,knob}.txt`、`lever_check.txt`。

---

## S4.5 — 分辨率敏感度扫描 ⛔ **卡在 S6 上，不是"下一步"**

`../plan/05` 实验零 §1.1 原文：**"擦拭 × Allegro × C4，表面点数
{64, 256, 1024, 4096}，3 seed。取下游成功率饱和点。"**
§1.2 物体位姿噪声也是"跑所有 Tier 1 checkpoint，找成功率拐点"。

⚠️ **两小节的判据都是"下游成功率"，而下游成功率需要一个训好的执行器**（E-T C4 /
Tier 1 checkpoint）。那是 S6 的产物。**编号 S4.5 排在 S4 之后是错的**——它读起来
像"S4 做完就能做"，实际做不了。2026-08-30 复核时确认此事（外部审查一度把它当成
"下一步"，那是被编号误导）。

**S4 阶段能先做的、也已经做掉的那一半**，是不需要训练的部分：

| 事项 | 状态 | 在哪 |
|---|---|---|
| 表面采样**嵌套**多分辨率（前 64 ⊂ 前 256 ⊂ 前 1024 ⊂ 4096） | ✅ | D-50，`test_surfaces.test_levels_are_prefixes` |
| 最粗那档（64 点）不丢部件 | ✅ | `test_all_parts_reachable_at_lowest_level` |
| 高分辨率热图无损池化到低档的 parent map | ✅ | `surfaces.parent` |
| **位姿误差 → region/mode 降级曲线**（不经过训练的那一路） | ✅ | **S4.6**，见上一节 |

嵌套是**前置条件**：非嵌套的四套采样会把"分辨率的影响"和"采样点换了位置的影响"
混在一起，扫出来的曲线读不出结论。这一条现在有单元测试守着。

**待 S6 有 checkpoint 之后再做**：

| 表面点数 | 近似 pitch | 成功率 | 训练耗时 |
|---:|---:|---|---|
| 64 | ~15 mm | | |
| 256 | ~7.8 mm | | |
| 1024 | ~3.9 mm | | |
| 4096 | ~2.0 mm | | |

- [ ] 饱和点已确定：_待填（需要 S6 的 E-T C4 checkpoint）_
- [ ] 该结论已同步写入 `../plan/02` §2 和 `../plan/07` §1
- [ ] 若需高分辨率，已实现"低分辨率全局 + 接触邻域局部"（`plan/02` §2.3）

**在饱和点定下来之前，`plan/02` §2 的表示维度按当前值走**（抽屉/擦拭 16 384、
旋钮 8 192），不因为"还没扫"就停下——S5 用的是热图与部件占比，对绝对点数不敏感。

记录：待 S6。

---

## S5 — Shared Structure Model

判据见 `../plan/03` §9。

| 项 | 目标 | 实测 |
|---|---|---|
| episode-level field coverage | 描述性统计先实测；region 同时报 ≥95% force-mass 覆盖率 | |
| width | ≤ oracle 的 1.5× | |
| 冻结 executor 成功率下降 | ≤10 pt | |
| 策略身份 probe 准确率 | 显著低于原始动作 | |
| 擦拭跨实现互换后下降 | ≤10 pt | |

记录：

### 2026-08-30 · S5 第一版 contract + 真实 S4 smoke（`interaction-transfer-v1`，**已作废**）

- 新增 `src/it/transfer.py`：exact allowlist、episode 等权的 32-bin 聚合、save/load；
  任务名只在审计 meta，不进 executor payload；
- 新增 `tools/s5_build_transfer.py` 与 5 条本地测试；失败示教、混任务、surface hash
  不一致、未知输入字段均 fail closed；
- 发现并修复 P-57：surface 点序必须作为 artifact 随 S4 数据冻结，不能只存 hash 后
  在另一 NumPy 环境重建。

⚠️ **v1 的两处结论已被同日的复核推翻**（详见下一节）：所谓"6D wrench 重建误差
6.18e-6 N"是代数恒等式、不构成验证（P-60）；而 v1 的命令轴与 per-cell 统计口径都是
错的（P-58 / P-59）。**v1 artifact 一律作废，读到即报错。**

### 2026-08-30 · 接手复核 → `interaction-transfer-v2`（D-65 / D-66）

在 33 条真实 S4 记录（抽屉 12 / 擦拭 12 / 旋钮 9）上复核 v1，发现两个**不报错、
只让指令失效**的缺陷，并据实测重做了契约。

**缺陷一：命令轴退化（P-58）。** v1 按任务完成度 `progress` 分格：

| 任务 | 帧落进"首格 + 末格"的比例 | 中间 30 格每格帧数中位数 |
|---|---:|---:|
| 抽屉 | **83.9%** | 2 |
| 旋钮 | **73.3%** | 2 |
| 擦拭 | 37.9%（另有 15.4% 空格） | 7 |

当时 `support/episodes` 满格、`command/valid` 全 True、`empty_bins` 报 0。

**缺陷二：同一 cell 的字段来自不同 episode 子集（P-59）。** region/engage/mode 用
NaN 排除没碰过的 episode，traction 用 0 算进去；每 cell 支持中位数只有 5/12，于是：

| 任务 | v1 中"该接触但力中位数恰为零"的 occupied cell 占比 | v2 |
|---|---:|---:|
| 抽屉 | **57.9%** | 0（契约硬约束） |
| 擦拭 | **80.7%** | 0 |
| 旋钮 | **48.7%** | 0 |

**命令轴选型实测**（`tools/s5_align_probe.py`，产物 `out/s5_align/probe.txt`）。
主判据 `binJS` = 格内逐帧接触分布的不一致度，**不受格子大小混淆**：

| key | 抽屉 binJS | 擦拭 binJS | 旋钮 binJS | 接触格占比（抽/擦/旋） | 空格率 |
|---|---:|---:|---:|---|---|
| `progress`（v1） | 0.145 | 0.305 | 0.210 | 99.5 / 84.6 / 100 | 0.3 / **15.4** / 0 |
| `phase_time` | 0.146 | 0.383 | 0.357 | **18.0** / 100 / **26.4** | 0 |
| `phase_progress` | 0.134 | 0.397 | 0.325 | 43.5 / 100 / 38.2 | 1.0 / 0 / **17.0** |
| `phase_effect` | 0.130 | 0.381 | 0.332 | 44.8 / 100 / 36.1 | 2.6 / 0 / **20.5** |
| **`activity`** ← 选中 | **0.114** | **0.260** | 0.215 | **87.2 / 100 / 88.5** | **0** |

如实记下不利项：`within_family_js` 在抽屉上略偏向 `progress`（0.301 vs 0.307）。
**没有按它选**，因为它奖励退化对齐（帧越集中、分布越平均越"像"）。见 D-65。

**v2 的改动**：命令轴改为"按 phase 分段 + 段内按累计交互活动量"；per-cell 字段一律
接触条件化；新增 `region/support`、`region/duty`、`engage/concentration`；诊断从
"wrench 重建误差"改为**表面投影完整性**（残差 + 被滤掉的力占比，两个都能失败）。
schema 提到 `interaction-transfer-v2`，v1 读到即报错。

**新增工具**：`tools/s5_align_probe.py`（对齐选型）、`tools/s5_freeze_surfaces.py`
（把 P-57 的冻结 surface 补写进既有 S4 数据集）、`tools/s5_eval_envelope.py`
（八项闸门检查）、`tools/s5_all.sh`（唯一入口，逐项收退出码，P-55）。
本地测试从 5 条扩到 9 条，三套单测全绿。

### 2026-08-30 · S5 全量构造与冻结留出评估（三任务）

完整结果与复现命令在 **`../out/s5/README.md`**，引用数字以那份为准。

| 任务 | train | 校准 | τ* | 1a′ conformal coverage（in-dist/物理/策略） | 2a width | 力集合带内率 |
|---|---:|---:|---:|---|---:|---|
| 抽屉 | 323 | 43 | 0.990 | ✅ 0.953 / 0.970 / 0.971 | **1.35%** | 0.968~0.989 |
| 擦拭 | 950 | 127 | 0.940 | ❌ 0.921 / **0.868** / 0.997 | 4.42% | 0.973~0.981 |
| 旋钮 | 652 | 87 | 0.930 | ❌ **0.892** / **0.896** / 0.941 | 1.37% | 0.966~0.978 |

标定烤进 artifact 之后（`interaction-transfer-v3`，D-67/D-71），mechanics 的带内率从
0.24~0.68 变成 **0.966~0.989**，episode 级同时带内率从 0.000 变成 0.24~0.72。
擦拭原来的 0.000 是 `direct_wipe` 的寄生接触，该档已按 D-70 移出范围；
现在剩下的 1a′ 不足都是边缘性的（0.868~0.896 对门槛 0.90）且落在分布漂移的划分上。

\* 擦拭那个 0.000 来自采集缺陷（D-69），不是方法结论。

**三条结论**：

1. **region envelope 标定后很紧**——抽屉允许区域只占表面积 **1.35%**，256 个 cell
   里平均 5 个，而留出 coverage 0.95~0.97。这是 `plan/03` §8.1 目标函数的第一组实测值；
2. **mechanics 的边缘刻度是准的，缺的是联合集合**（⚠️ 此条此前写反，见 P-65）——
   早先的"带内率 0.24~0.68"要求三分量**同时**命中；逐轴分开测是 **0.75~0.89**
   （名义 0.80），三轴独立时联合本就该是 0.8³≈0.51，实测 0.52/0.77/0.61 还略高于
   乘积。**物体系三维 traction 表示没问题**，缺的是从未构造过联合允许集合。
   集合形状由 `tools/s5_mech_setform.py` 在同一 coverage 目标下比体积实测决定；
3. **没有严重多峰**——质心到示教 / 示教两两 = 0.84 / 0.96 / 0.92，均 < 1，
   质心落在示教分布内部。D-63 预留的聚类/条件模型这条路暂时不用走。
   ⚠️ 早先用最近邻当分母得到 3.01~36.9 的假信号，见 P-61。

**两处不得当成方法结论的失败**：擦拭 `direct_wipe` 的 0.000 是寄生接触（D-69/P-64，
未使用的黑板擦全程压在板角，占 ~10% 的力，60/60 条 episode 都有）；子群 coverage 的
落差（抽屉 `pinch_offset` 0.833、旋钮 `pin_push_dual` 0.685）则是**真结果**——
conformal 只保证 marginal coverage，对子群无条件保证（D-59），实测证实了这一点。

**顺带证实**：同部件池化（D-68）让 envelope 同时更准更紧——抽屉 width 2.25% → **1.35%**，
擦拭未见策略 coverage 0.655 → **0.997**、width 5.86% → 4.42%。
P-57 也得到直接确证：本机重算的抽屉/旋钮 surface 与服务器冻结的那份 points 不一致。

**新增工具**：`s5_parasite_check.py`（寄生接触检查，已接进 `s5_all.sh`；实测在
`direct_wipe` 上 FAIL、其余 **19 个分组全部 PASS**）、`s5_fetch.sh`（取回文本产物）。

---

## S6 — E-I 交互跟踪执行器 ⚠️

**本轮最关键的一步。** `../plan/04` §5。

| 执行器 | 训练指令来源 | 留出任务 | 跟踪成功率 | interaction coverage / 失败模式 | 状态 |
|---|---|---|---|---|---|
| Allegro | 预训练集 + 抽屉 + 旋钮 | 擦拭（两种实现） | | | ⬜ |
| **垫头杆** | **仅预训练集** | 擦拭 | | | ⬜ |
| 平行夹爪 | 预训练集 + 抽屉 | 擦拭 | | | ⬜ |
| 钩杆 | 仅预训练集 | 抽屉 + 旋钮 | | | ⬜ |

通过条件：训练覆盖内 interaction 跟踪成功率 ≥80%，并在冻结的未见物理指令上报告
距离、局部密度、跟踪率与失败模式。若另启用 evaluator，AUC/calibration/selection
regret 只作为该可选模块自己的验收，不再绑进主链。

- [ ] **E-I reward 中不含任务 success bonus / dirt 清除量 / 任务关节目标**（`plan/04` §5.1 禁止清单）
- [ ] **dataloader 断言已生效**：留出任务在 A–D 任何阶段都不出现（不靠自觉，靠断言日志）
- [ ] **E-I exact allowlist 已生效**：拒绝 `mech/generalized`、任务原生 effect、
      phase/progress、task id/name（D-58）

记录：

---

## S7 — 留出任务零样本（executor 强泛化闸门）⭐

`../plan/05` 实验二，Gate E。

| 执行器 | 留出任务 | 零样本成功率 | 有评价器 | 无评价器 | 通过 |
|---|---|---|---|---|---|
| Allegro | 擦拭(工具) | | | | |
| Allegro | 擦拭(直擦) | | | | |
| 垫头杆 | 擦拭 | | | | |
| 夹爪 | 擦拭 | | | | |
| 钩杆 | 抽屉 | | | | |
| 钩杆 | 旋钮 | | | | |

**Gate E**：至少两个执行器零样本 ≥60%，且其中至少一个是垫头杆/钩杆这类 0 自由度形态。

若训练任务好、留出任务差 → 先按冻结协议检查 command 最近邻距离、kNN 密度、
未见 pairwise 组合与 policy 跟踪，**不得把留出任务或按它
反推的新 probe 加进原 Gate E 训练**。如需扩展数据，必须先封存并报告原 Gate E 失败，
新增 D-xx，把扩展版作为后续实验，不能倒写成原协议通过（D-60）。

记录：

---

## S8 — E-T 信息条件对照

`../plan/04` §11 分层，合计约 147 次训练（含 S2/S4.5/S6）。

### Tier 1（不可裁）：擦拭 × {Allegro, 垫头杆} × C0–C5 × 5 seed = 60

| 执行器 | C0 | C1 | C2 | C3 | C4 | C5 |
|---|---|---|---|---|---|---|
| Allegro | | | | | | |
| 垫头杆 | | | | | | |

### Tier 1s：稀疏 reward × {C0, C2, C4} × 3 seed = 18

| 执行器 | C0 | C2 | C4 |
|---|---|---|---|
| Allegro | | | |
| 垫头杆 | | | |

### Tier 2：旋钮 × {Allegro, 钩杆} × {C0, C2, C4} × 3 seed = 18

### Tier 3：抽屉 × {Allegro, 钩杆, 夹爪} × {C0, C4} × 3 seed = 18（阴性对照）

进度紧张时的裁剪顺序：Tier 3 降到 1 seed → Tier 2 砍 C2 → **Tier 1 与 E-I 不可裁**。

---

## S9 — 扰动恢复

`../plan/05` 实验四。**纯评估，用 S8 的 checkpoint，不需新训练。**

| 扰动 | C0 恢复率 | C2 | C4 | 纠正方向正确率 (C0/C4) |
|---|---|---|---|---|
| 摩擦 ×0.5 | | | | |
| 摩擦 ×2 | | | | |
| 质量/阻尼突变 | | | | |
| 外部冲量 | | | | |
| 诱导滑移 | | | | |

**Gate D'**：C4−C0 的恢复率差距显著大于无扰动时的成功率差距。

不通过不阻断后续，但削弱硬件必要性论证，须写进论文限制一节。

---

## S10 — 评估与报告

| 闸门 | 判据 | 实测 | 结论 |
|---|---|---|---|
| Gate A | A1 脚本可行；A2 Expert 按需证明可学，不称性能上限 | | |
| Gate B | C4 下可行组合 ≥80% | | |
| Gate C | region/mode/mechanics 各有一个反事实产生物理一致的特定失败 | | |
| Gate D | **C1→C2 或 C2→C4** 有跨 seed 一致的可信提升 | | |
| Gate D' | 扰动下 C4−C0 差距放大 | | |
| **Gate E** | **≥2 执行器留出任务零样本 ≥60%，含 1 个 0-DoF；配 CI、command 距离/密度曲线与可比基线** | | |
| Gate F | ≥3 种形态实现同一未改写 envelope | | |
| Gate G | 抽屉阴性结果可接受（不修改任务追求差距） | | |

**统计口径**（`plan/05` §9）：Tier 1 报 5 seed 均值 + 标准差 + bootstrap 置信区间；Tier 2/3 报 3 seed。**不以任意百分点硬阈值作为唯一判据**，而看置信区间、跨 seed 一致性和物理解释。

人工检查清单（`../plan/06` §7）每次 eval 都要走一遍：

- [ ] 看过 ≥5 条成功视频、≥10 条失败视频
- [ ] 看过四宫格，且四格 envelope 文件 hash 一致
- [ ] 确认 counterfactual 只改了指定字段，失败来自物理而非脚本崩
- [ ] 确认不同信息条件用同一批 episode
- [ ] **确认 E-I 留出任务未出现在训练数据里（查断言日志）**
- [ ] 确认没有根据确认集结果继续调场景

记录：
