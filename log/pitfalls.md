# 踩坑记录

格式：**现象 → 原因 → 解法**。只记现象不记解法等于没记。

下面 P-01 ~ P-10 是**开工前就已知的坑**（来自文档和社区反馈），先写在这里避免重踩。之后按 P-11 往下追加。

---

> ## ⚠️ 2026-08-27 实测复核（Isaac Lab 2.3.1 / Isaac Sim 5.1.0-rc.19）
>
> P-01 ~ P-10 是针对**旧版**写的。已在服务器上跑 `/tmp/verify_contact.py` 逐条复核，结论如下。**以本表为准，正文中失效的条目已就地标注。**
>
> | 条目 | 复核结论 |
> |---|---|
> | P-01 activate_contact_sensors | ✅ **仍适用**。实测 `net_forces_w = [0, 0, 4.9050] N` |
> | P-02 track_friction_forces | ❌ **解法失效**，该配置项在 0.48.0 **不存在**（版本过旧）。现行解法见 **P-16** |
> | P-03 max_contact_data_count 默认 4 | ✅ **仍适用**。内省确认默认值 = 4 |
> | P-04 contact_pos_w 出现 NaN | ⚠️ **归因需修正**：NaN 不是 bug，是"该碰撞对未接触"的**哨兵值**。见下方修订 |
> | P-05 force_matrix_w 返回 None | ✅ **可规避**：设了 `filter_prim_paths_expr` 后正常返回 `shape=(2,1,1,3)` |
> | P-06 ~ P-08 | 未复核（S0 时验） |
> | P-09 力必须有物理意义 | ✅ **实测通过**：`\|F\|/mg = 1.0000` |
> | P-10 按 episode 划分 | 与仿真器无关，仍适用 |
>
> **新增坑：P-16（摩擦力接口）、P-17（filter 目标必须是刚体）、P-18（buffer 按 env 打包）。**
>
> 另外：`activate_contact_sensors=True` **只能加在有 rigid body 的 prim 上**。加在静态碰撞体（只有 `collision_props`、无 `rigid_props`）上会直接抛
> `ValueError: No contact sensors added to the prim`。
>
> 验证脚本留在服务器 `/tmp/verify_contact.py`（v1）、`/tmp/verify3.py`（v3，最终版）。**进代码库时移到 `tools/check_contact_sensor.py`。**

---

## P-01 · 接触传感器全是零

**现象**：`ContactSensor` 读出来的力全是 0，明明物体在接触。

**原因**：PhysX 的 ContactReporter 没激活。

**解法**：在**资产的 spawner**（不是 sensor cfg）上加 `activate_contact_sensors=True`。

---

## P-02 · 判不出 stick / slide ⚠️ 解法已失效，见 P-16

**现象**：切向力永远是 0，无法区分粘滞和滑动。

**原因**：`net_forces_w` **只是法向接触力之和**，不含切向分量。实测确认：静止时 `net_forces_w = [0, 0, 4.9050]`，切向分量恒为 0。

**旧解法（在 0.48.0 上无效）**：~~`ContactSensorCfg(track_friction_forces=True)`~~ —— **该配置项不存在**。

**现行解法**：见 **P-16**。

---

## P-03 · 接触点静默丢失

**现象**：接触点数量明显偏少，一致性校验（`plan/02` §7）残差异常大。

**原因**：`max_contact_data_count_per_prim` 默认只有 **4**，contact-rich 场景必然溢出，而且**不报错**。

**解法**：调到 64。这是首选排查项——一致性校验标记率高时先查这个。

---

## P-04 · contact_pos_w 出现 NaN ⚠️ 归因已修正（2026-08-27 实测）

**现象**：数据链里冒出 NaN，训练直接崩。

**~~旧归因~~**：~~reset 后被填成 `torch.nan`~~ —— 不准确。

**真实原因**：**NaN 是"该碰撞对当前未接触"的哨兵值，是设计如此，不是 bug。** 本地源码 docstring 原文：

```python
contact_pos_w: torch.Tensor | None = None
"""Average of the positions of contact points between sensor body and filter prim in world frame.
   Shape is (N, B, M, 3) ...
   Collision pairs not in contact will result in NaN.
"""
```

任何时刻只要该 (sensor, filter) 对没有接触，就是 NaN，与 reset 无关。

**解法**：

1. **把 NaN 当信息用**：`torch.isnan(contact_pos_w)` 直接就是 `no-contact` 判据，正好对应 `plan/02` §3.4 的第一档，不需要另外设力阈值；
2. 进入任何数值运算前 `torch.nan_to_num()` 或掩码，否则会顺着策略梯度传播；
3. **若观察到"明明在接触却全是 NaN"，先查 P-17**——多半是 filter 目标不是刚体，压根没配上对。

---

## P-05 · force_matrix_w 返回 None

**现象**：想拿"某个 body 对某个物体"的成对力，拿到 `None`。

**原因**：Isaac Lab 目前只支持 **many-to-one** 的接触力过滤；覆盖多个 body 的 sensor 上直接返回 `None`。

**解法**：要么拆成多个单 body sensor，要么改用 `ContactSensor.contact_physx_view.get_contact_data()` 取原始数据自己配对。

---

## P-06 · headless 下没有画面/录不出视频

**现象**：加了 `--video` 但没有 mp4，或者报渲染错误。

**原因**：headless 默认加载 `isaaclab.python.headless.kit`，没有渲染能力。

**解法**：必须同时加 `--enable_cameras`，它才会切到 `isaaclab.python.headless.rendering.kit`。另外确认 `ffmpeg` 已安装。

---

## P-07 · `--headless --video` 卡死

**现象**：训练脚本启动后无输出、无进程退出，尤其在容器里。

**原因**：社区有多起报告，与内部 `enable_cameras` 赋值 / 容器环境有关，未完全定位。

**解法**：**训练时不录像**，用独立的 eval 脚本录（`plan/06` §1.3）。不要在训练脚本里死磕。

---

## P-08 · 吸盘（SurfaceGripper）不能用于 RL 训练

**现象**：用了 Isaac Sim 的 `SurfaceGripper`，只能开很少的并行环境。

**原因**：截至 Isaac Sim 5.0，**Surface Gripper 只支持 CPU 后端**，官方 tutorial 要求 `--device=cpu`。

**解法**：本预演已改用钩杆，不涉及。若将来要做吸盘，自己实现"带断裂阈值的 6-DoF 软约束"（PD 外力 + latch/unlatch），全程 GPU 友好。见 `decisions.md` D-03。

---

## P-09 · 采集板的接触力没有物理意义

**现象**：板能施加任意大的力，Mechanics 字段数值离谱。

**原因**：把板设成了 kinematic body。kinematic body 不参与动力学求解，能施加无限大的力。

**解法**：板必须是**普通刚体 + PD 外力驱动**（`plan/01` §4.1）。自检判据：自由落体后稳态接触力 ≈ mg。

---

## P-10 · 测试成绩虚高

**现象**：test 成功率异常好，但换一批数据就崩。

**原因**：数据集按**帧**划分 train/test，同一条轨迹的相邻帧同时出现在两边。

**解法**：必须按 **episode** 划分（`plan/03` §7）。

---

下面 P-11 ~ P-15 是 2026-08-27 修订时识别出的**方法学坑**，不是工程坑。它们不会让程序崩，只会让结论作废，因此更危险。

---

## P-11 · Region 其实是从 effect 推出来的

**现象**：C1（+区域）相比 C0 毫无提升，或者提升不稳定。

**原因**：对刚性单自由度铰接物体，`effect + 物体几何` 在构造上几乎蕴含 region——销钉在点云里直接可见，转轴由 effect 推出。执行器不需要任何人告诉它去哪接触。这不是"捷径没堵死"，是这类物理系统本身如此。

**解法**：

1. 跑 `plan/02` §7 第 9 条的**可推导性探针**（只看 effect + 点云预测 region）先确认；
2. 若确认冗余，要么让"几何可达但功能后果不同"的区域存在（旋钮的轮缘低摩擦，D-14），要么如实报告该任务不适合检验 region；
3. **不要靠调随机化范围来制造差距**——那是调到 baseline 失败为止。

---

## P-12 · E-I 的 reward 里混进了任务

**现象**：E-I 在留出任务上零样本成功率意外地高，或者训练时收敛得意外地快。

**原因**：任务信息从后门进来了。常见途径：

- reward 里保留了 success bonus 或 dirt 清除量；
- effect 目标从环境配置读取而非从指令通道读取；
- 环境 reset 逻辑按任务分支，执行器能从初始状态分布反推任务；
- curriculum 里为了调试临时加了任务项，忘了删。

**解法**：`plan/04` §5.1 有明令禁止清单。在 dataloader 和 reward 函数里都加**断言**，并把断言日志存档——`plan/06` §7 要求每次 eval 前查这个日志。不靠自觉。

---

## P-13 · 留出任务差的时候，把它加进训练

**现象**：数字突然好看了。

**原因**：这是本项目最容易破防的地方。留出任务差 → 加数据 → 立刻好看 → 主张归零，且很难被自己发现。

**解法**：留出任务差时的**唯一**允许动作是扩预训练物体集（`plan/03` §2.4）。扩两轮仍不成立，如实报告 Gate E 未通过。写进 `decisions.md`，不要静默处理。

---

## P-14 · Envelope 靠"变宽"通过验收

**现象**：coverage 轻松达到 95%+，下游成功率也不错。

**原因**：envelope 宽到几乎不约束任何东西。只报 coverage 时这完全看不出来。

**解法**：**coverage 和 width 必须同时报**（`plan/03` §8.1、`plan/06` §2.3）。目标是 coverage ≥90% 约束下最小化 width。另外 mechanics 范围不要用"均值 ± k·标准差"——接触力分布通常单侧截断，对称假设不成立，用 pinball loss 回归分位数。

---

## P-15 · 四宫格四格用了不同的 envelope

**现象**：跨形态视频看起来很漂亮。

**原因**：为了让每一格都成功，各自用了各自 target 上表现最好的那条 envelope，或者对 envelope 做了 target-specific 的微调。这样整张图的意义就没了。

**解法**：四格必须来自**同一个 envelope 文件**，在视频里标出该文件的 hash（`plan/06` §3.1、§7）。允许 target 在 envelope 的允许集合内选择具体实现（那正是可行性评价器做的事），但**不允许把值改到允许集合之外**——后者属于 embodiment-specific adaptation，必须单独成节报告（`plan/05` §4.2）。

---

下面 P-16 ~ P-18 是 2026-08-27 在服务器上实测踩到的**真实工程坑**。

---

## P-16 · 拿不到摩擦力（stick/slide 判不出）

**现象**：`ContactSensorCfg` 里没有 `track_friction_forces`，`ContactSensorData` 里没有任何摩擦力字段。

**原因**：**版本过旧。** 该功能由 [PR #3563](https://github.com/isaac-sim/IsaacLab/pull/3563) 引入，**2025-12-10** 合并进 main。本地 isaaclab 版本 0.48.0，changelog 日期 **2025-11-10**，早一个月。不是被移除，是还没加进来。

**解法**：绕过 Isaac Lab 包装层，直接调底层 PhysX tensor view：

```python
# 前提：ContactSensorCfg 必须设 filter_prim_paths_expr（非空）
#       且 max_contact_data_count_per_prim >= 1
ff, fpts, counts, start = sensor.contact_physx_view.get_friction_data(dt=sim_physics_dt)
```

**实测验证**（`/tmp/verify3.py`，mass=0.5 kg，μ=0.4，μmg=1.962 N）：

| 施加力 | 摩擦力(单 env) | 速度 | 模式 |
|---:|---:|---:|---|
| 1.0 N | 1.000 N | ~0 | STICK（静摩擦跟随外力） |
| 1.5 N | 1.500 N | ~0 | STICK |
| 2.5 N | **1.962 N** | 0.54 m/s | SLIDE（饱和到 μN） |
| 3.0 N | **1.962 N** | 1.04 m/s | SLIDE |

教科书级库仑摩擦。**stick/slide 判据**：`|Ff| < μN` → stick；`|Ff| ≈ μN 且 v_tangential > 0` → slide。

**为什么不升级**：upstream 的 `friction_forces_w` 形状是 `(N,B,M,3)`，是**按 filter body 聚合求和**的，把空间分布抹掉了；而 `plan/02` §3.2 要的是逐接触点的力+位置+法向。升级之后仍然要调这两个底层接口。见 `decisions.md` D-20。

---

## P-17 · filter 相关的数据全是零 / 全是 NaN ⚠️ 最容易误判成 Isaac Lab bug

**现象**：物体明明压在地板上，`net_forces_w` 也正确（= mg），但是：

- `force_matrix_w = [0, 0, 0]`
- `contact_pos_w` 100% NaN
- `get_friction_data()` 全零
- `get_contact_data()` 的 **`counts = [0, 0]`**

**原因**：**filter 目标不是 rigid body。** PhysX 的成对接触过滤要求**两侧都是刚体**；只有 `collision_props` 而无 `rigid_props` 的静态碰撞体不会注册成可过滤的接触对。

`net_forces_w` 仍然正确，因为它**不走 filter 通道**——这个不一致极具迷惑性，会让人以为是传感器 bug。

**解法**：所有 filter 目标建成刚体。不动的东西（地板、桌面、柜体）用：

```python
rigid_props=sim_utils.RigidBodyPropertiesCfg(kinematic_enabled=True)
```

**首选排查项**：拿不到 filter 数据时，第一个看 `get_contact_data(dt)[4]`（counts）。是 0 就是没配上对，不用再往下查。

**对本项目的影响**：`plan/01` §1 已加硬规则 7。擦拭平面、抽屉柜体如果建成静态碰撞体，**接触区域和摩擦力全部拿不到**，`plan/02` 的 Interaction Region 和 Contact Mode 两个字段直接作废。

---

## P-18 · 逐点接触数据是按 env 打包的，直接求和会串环境

**现象**：摩擦力/接触力数值恰好是预期的 N 倍（N = env 数）。

**原因**：`get_contact_data()` / `get_friction_data()` 返回的是**扁平打包 buffer**，形状 `(max_contact_data_count, 3)`，其中 `max_contact_data_count = max_contact_data_count_per_prim × num_envs × num_sensor_bodies`。所有 env 的接触点挤在同一个数组里。

实测：2 env × 每 env 4 个接触点 → 8 行非零，`counts = [4, 4]`，`start_idx = [0, 4]`。直接 `.sum(dim=0)` 会把两个 env 加在一起，正好差 2 倍。

**解法**：用 `counts` 和 `start_idx` 切片：

```python
forces, points, normals, seps, counts, start = view.get_contact_data(dt=dt)
for e in range(num_envs):
    s, n = start[e, 0].item(), counts[e, 0].item()
    env_points  = points[s:s+n]      # 该 env 的接触点
    env_forces  = forces[s:s+n]
    env_normals = normals[s:s+n]
```

**这层切片逻辑应该封装成项目自己的工具函数**（约 30 行），产出 `plan/02` §3.2 需要的"逐接触点 位置 / 法向 / 法向力 / 摩擦力"，不要在每个环境里重复写。

---

## P-19 · headless 下 `sim.step()` 卡死、`app.close()` 挂起

**日期**：2026-08-27（S1 开发时踩到）

**现象**：脚本跑到第一次物理步进就停住。进程还在，但 **CPU 0%、日志不增长、GPU 显存已分配但利用率 0%**。等 15 分钟也不动。

**原因**：两个独立问题，都出在渲染路径上。

1. `SimulationContext.step()` 默认 `render=True`。headless 且未加 `--enable_cameras` 时没有渲染能力，调用会阻塞。这和 P-06/P-07 是同一类问题，只是发生在 API 层而不是命令行层。
2. `SimulationApp.close()` 在本环境同样会挂起。最小复现脚本十步全部跑完、打印完最后一行，仍然 `timeout 124`——卡在关闭阶段。

**解法**：

```python
sim.step(render=False)      # 所有不需要出图的地方一律关渲染

...
# 报告落盘后直接退出，不走优雅关闭
sys.stdout.flush()
os._exit(exit_code)
```

`os._exit()` 跳过 atexit 和析构，对自检/评估脚本没有副作用（结果已经写文件了）。真正需要录像的评估脚本另说，那时必须加 `--enable_cameras`（P-06）。

**排查提示**：区分"卡死"和"慢"看 CPU——慢是 100%，卡死是 0%。本项目里 0% CPU + 显存已占，基本就是渲染阻塞。

---

## P-20 · `pkill -f <pattern>` 会杀掉自己所在的 shell

**日期**：2026-08-27（连踩两次）

**现象**：`ssh host 'pkill -f foo.py; cat > /tmp/foo.py'` 之后文件没被写入；或者 `pgrep -f foo.py` 明明进程已结束却一直报"运行中"。

**原因**：`pkill -f` / `pgrep -f` 匹配**完整命令行**，而执行它的那个 `bash -c` 的命令行里就含有这个模式字符串，于是匹配到自己。`pkill` 把自己所在的 shell 杀掉，后续命令全部不执行；`pgrep` 则永远至少匹配到 1 个。

**解法**：

```bash
pgrep -f "[f]oo.py"     # 字符类：模式本身不含字面 "foo.py"
```

或者把 kill 和后续操作拆成两条独立的 ssh 调用。

**代价**：这个坑最恶心的地方是**它静默失败**——第二次踩到时，我以为是资产生成有问题，实际上是生成脚本压根没被写进去。看到"命令明明跑了但文件没变"，先怀疑这个。

---

## P-21 · `set_joint_effort_target` 会一直保持，不会自动清零 ⚠️ 最难查的一个

**日期**：2026-08-28（S1，查了两小时）

**现象**：前一段测试里给关节施加过力矩，之后的测试中物体的**几何位置全错**——按配置算出的目标位姿完全对不上，执行器"穿过"物体却读不到任何接触。

**原因**：Isaac Lab 的 `set_joint_effort_target()` 设的是**持续指令**，不调用就一直保持上次的值。S1 里旋钮的"已知力矩响应"测试施加 1.5 N·m 把圆盘推到限位 3.491 rad（=200°），之后的摩擦标定阶段圆盘**仍在被 1.5 N·m 驱动**，销钉早就不在角度 0，而标定的目标位姿是按角度 0 算的。

**为什么极难定位**：

- 轮缘那一半**照常工作**（圆盘轴对称，转到哪都一样），只有销钉那一半失败——看起来像"销钉碰撞体有问题"；
- 单独测销钉碰撞体（扔球）**完全正常**；
- `write_joint_state_to_sim(0, 0)` 复位后下一步就又被推回去了，看不出来。

**解法**：换阶段时**显式清零**，并在新阶段的每一步持续清零：

```python
zero = torch.zeros(num_envs, num_joints, device=device)
knob.set_joint_effort_target(zero)
knob.write_joint_state_to_sim(zero, zero)
# 新阶段的每一步也要
def step_fn(i):
    knob.set_joint_effort_target(zero)
    ...
```

**排查提示**：几何对不上时，先打印物体**当前实际关节角**，不要相信"我 reset 过了"。

---

## P-22 · 接触力峰值包含撞击瞬态，不能当"稳态能传多少力"用

**日期**：2026-08-28

**现象**：明明指令法向力 25 N，实测峰值 66.5 N。

**原因**：物体接触瞬间的冲击尖峰。用峰值去论证"在安全力上限内能传多少力矩"，前提直接不成立——因为力早就超了上限。

**解法**：统计时过滤掉超出指令值的样本：

```python
if fnv > MAX_NORMAL_FORCE * 1.15:
    continue          # 撞击尖峰，不计入
```

过滤后旋钮的峰值法向力从 66.5 降到 28.15 N，落在上限内，结论才站得住。

**残留问题**：即便过滤后，τ_pin 实测 23.48 N·m vs 解析 1.30 N·m 仍差一个量级——峰值里还有求解器瞬态。**定性排序（rim < need < pin）可靠且裕量充足，但具体数值不可引用**。要写进论文需改用稳态窗口（接触建立后的中位数）重测。

---

## P-23 · 自由漂浮体在没有控制器接管的阶段会一直掉

**日期**：2026-08-28

**现象**：某个阶段结束后，浮动执行器的位置离谱（实测推子掉到 z = -30 m）。

**原因**：浮动刚体只在被 PD 驱动的阶段受控。前面的阶段如果没人管它，它就自由落体，掉多远取决于那个阶段多长。

**解法**：每个控制阶段开始前显式重置（`tools/s1_check.py::_reset_floating`），不要指望它还在原地。

---

## P-24 · headless 渲染的三个坑

**日期**：2026-08-28（S0）

**现象与解法**：

| 现象 | 原因 | 解法 |
|---|---|---|
| `RuntimeError: A camera was spawned without the --enable_cameras flag` | `SimulationApp({"enable_cameras": True})` **不起作用**。Camera 检查的是 carb 键 `/isaaclab/cameras_enabled`，只有 `AppLauncher` 会写它 | 用 `AppLauncher(headless=True, enable_cameras=True)`，它同时负责选 `isaaclab.python.headless.rendering.kit`（P-06） |
| 开了 cameras 后 `sim.step(render=True)` 仍卡死 | 与 P-19 同源 | 物理走 `step(render=False)`；出图时单独 `sim.render()` + `cam.update(dt)` |
| `cam.set_world_poses_from_view()` 卡死 | 未定位 | 相机位姿在 `CameraCfg.OffsetCfg(pos=..., rot=..., convention="opengl")` 里静态给定，四元数用 `tools/s0_record.py::look_at_quat()` 算 |

**顺带**：只设物理材质不设视觉材质，渲染出来全是白的，**功能区域完全看不出区别**。旋钮的轮缘和销钉一个颜色，而它们的区别就是 D-14 的全部内容。`src/it/build_assets.py::COLOR` 已给所有资产配色。

---

## P-25 · 可视化脚本的动作设计错了，会让正确的结论看起来是错的

**日期**：2026-08-28（S0）

**现象**：S1 数字全对，但录出来的视频要么看不出在验证什么，要么给出相反印象。

**三个实例**：

1. **推子飞出画面**：轮缘那段数字正确（圆盘没转），但画面里推子飞走了，观众合理地理解成"推子飞了所以没转"，而不是"摩擦不够所以没转"。
2. **两种情况用了同一条轨迹**：把销钉也写成"径向压紧 + 沿圆周走"——力穿过转轴，力矩恒为零，Δθ=0.0000。**视频看起来像销钉也推不动，与 D-14 的结论完全相反。**
3. **让推子追着销钉走**：推子的位置目标随圆盘一起动，两者互相拉扯，只转到 +0.13 rad（直线推法能到 +1.52）。

**解法**：可视化的动作必须**忠实反映被验证的物理**，不能图省事统一。旋钮的两种情况本来就不同：

```
销钉：切向直推      τ = F·r        与 μ 无关
轮缘：径向压+切向拖  τ = μ·F·R      只能靠摩擦
```

**教训**：视频不是"把数字配个画面"，它是独立的一道检查。`plan/06` §7 要求人工看视频，正是因为它能抓到数字抓不到的东西——但前提是**视频本身得是对的**。

---

## P-26 · 开环推动的最终转角对推力**非单调**，不能当稳健指标

**日期**：2026-08-28（S0 复核用户质疑时发现）

**现象**：用直线推法推销钉，改变法向力：

| 法向力 | 最终 Δθ |
|---:|---:|
| 10 N | +0.234 rad |
| 15 N | +0.392 rad |
| 20 N | **+0.217 rad** ← 比 15 N 还少 |
| 25 N | +1.516 rad |

**原因**：结果不由"力够不够"决定，而由**接触维持多久**决定。开环直线推时，推子沿固定方向被力控加速；圆盘一转，销钉沿切向离开推子的位置窗口，接触断开。**力越大推子加速越快、越早脱离**，传递的总冲量反而可能更小。这是接触断裂时机主导的准混沌过程，对参数敏感。

**这不影响 S1 的结论**：D-14 的判据是**接触期间传递到轴的力矩**（τ_rim=0.170 / τ_need=0.420 / τ_pin=23.5），不是最终转角。力矩在接触瞬间测量，与接触持续多久无关。转角只在 S0 录像里作示意。

**这也不是任务设计的问题**：RL 训练出的策略是闭环的，会主动维持接触；只有我这段开环脚本才会脱开。但**不能拿开环转角当任何结论的依据**。

**教训**：报告里引用任何数字之前，先问"它对参数敏感吗"。扫一下就知道了，成本很低。用户的一句质疑（"是不是阻力太大"）直接暴露了这个问题——**质疑值得认真扫一遍，而不是拿解析式糊过去**。我最初的解析估算（τ_need/r = 8.1 N）就是错的，实测 10 N 远远不够。

---

## P-27 · 接触力尖峰 4384 N 把奖励曲线炸掉，而平均值只有 13 N ⭐

**日期**：2026-08-28（S2 训练调试）

**现象**：PPO 训练 239 轮曲线全程不收敛，在 **+55 和 −256 之间来回甩**，
从头到尾没稳过。评估显示 checkpoint 100 成功率 36.4%，checkpoint 200 反而 **0%**。

**排查过程（三次猜测全错，第四次靠测量）**：

我先后猜了「奖励量纲失衡」「提前终止刷分」「sign() 造成悬崖」——
都改了，都没解决。因为**我只有总 reward，看不见是哪一项在爆**。

加上分项记录之后一次就定位了：

```
diag/contact_force_max_N:  4383.6   ← 峰值
diag/contact_force_N:        12.9   ← 均值
```

**原因（两层）**：

1. **表层**：力惩罚 `w_force × (力 − 上限)` 是**无界**的。4384 N 的尖峰
   单步就扣掉 (4384−25)×0.05 ≈ 218 分。曲线的 −256 就是这么来的。
2. **根因**：`max_depenetration_velocity=5.0` 允许求解器用 5 m/s 的速度把
   互相穿透的物体推开，冲量巨大。**这才是 4384 N 的来源**，不是控制器给的力
   （PD 的 max_force 只有 180 N）。

**解法（两层都要修，只修表层等于掩盖）**：

```python
# 表层：惩罚封顶
over = (force - max_force).clamp(min=0.0, max=20.0)

# 根因：限制穿透恢复速度
sim_utils.RigidBodyPropertiesCfg(
    max_depenetration_velocity=1.0,   # 默认 5.0
    linear_damping=0.05, angular_damping=0.05,
)
```

**效果**：

| | 峰值接触力 | reward 曲线 |
|---|---:|---|
| 修复前 | 4383.6 N | `15, -256, -190, -52, 4, 33 … -160, -91, 17` 全程乱甩 |
| 只封顶 | 4383.6 N | `58, 31, 14, -7 … 54, 55, 50, 49, 47, 45, 47` 稳在 +45~55 |
| 两层都修 | **17.3 / 5.1 N** | 落进 25 N 安全上限内 |

**教训（这条最重要）**：

**奖励分项记录应该在写 reward 的时候就加上，不是等训练不收敛了再补。**
我在前面几轮一直强调「别猜，去测」，结果自己在奖励调试上猜了三轮。
成本是 239 轮作废的训练 + 三次无效修改。

所有新任务环境的 `_get_rewards` 都必须写进 `extras["log"]`：
每一项的均值 + 关键诊断量（接触力峰值、物体状态）。

---

## P-28 · 成功率 100% 的策略在「捅一下让物体自己滑」 ⭐

**日期**：2026-08-28（用户看录像时质疑，实测证实）

**现象**：抽屉×钩杆 Expert 固定/随机环境均 **100%** 成功，画面上抽屉确实被拉开了。
但用户看录像后说：「棍子是往抽屉靠的而不是往外拉，就像棍子靠墙慢慢滑下来，
而墙却反常地跟着棍子方向被拉走」。

**实测（逐控制步记录开度/速度/接触力）**：

| 步 | 速度 mm/s | 接触力 N | 接触点 |
|---:|---:|---:|---:|
| 18–30 | +196 → +214 | 6–13 | 1 |
| **36–48** | **+185 → +48** | **0** | **0** |
| **108–132** | **+238 → +70** | **全 0** | **全 0** |

**抽屉打开的过程中，60% 的控制步接触力为零。** 无接触段的速度衰减
（238 → 70 mm/s 用 0.48 s）反解出阻尼 **3.06**，正是配置的 `damping=3.0`
——确认是**自由滑行**。

**根因**：滑行距离 = v·m/c = 0.2 × 1.2 / 3.0 = **80 mm**，而抽屉总行程才 180 mm。
**一次冲量能滑完大半**，策略根本不需要持续施力。

**修复**：阻尼 3.0 → **30.0**，滑行距离降到 8 mm。

**修复后暴露的第二个问题**：阻尼提高后脚本验证从 180 mm 掉到 **3.6 mm**，
钩杆飞到 319 mm 外。因为原设计让横钩指 +Y 纯避让，拉力全靠
**主杆圆柱压把手圆柱**——两根轴垂直的圆柱是**点接触**，力一大就滑脱。

**第二次修复**：改成三段式**真正勾住**——下探时横钩转 +Y 避让，到位后绕 Z
转 90° 让横钩伸到把手杆**下方**，再拉。主杆压把手背面提供拉力，
横钩挡住向上脱出 → **形封闭**。结果 100% 达标，接触力 13–27 N。

**两条教训**：

1. **成功率不能替代人工看画面。** 100% 的策略在做一件不是操作的事。
   数字、甚至「无穿模无瞬移」都看不出来——是用户盯着录像发现的。
2. **物理参数会决定策略学到什么。** 阻尼太小，「捅一下」就是最优解，
   任何 reward 设计都挡不住。**要让策略学会持续接触，得让环境物理上
   要求持续接触**，而不是去写「必须一直接触」的奖励项。

---

<!-- 从 P-29 开始追加实际踩到的坑 -->
## P-29 · 不 pin GPU 时，第一次施加外力就 CUDA illegal memory access ⭐

**日期**：2026-08-28（S3 起步，卡了整整一轮）

**现象**：任何往浮动刚体上施加外力的脚本，在第一次
`scene.write_data_to_sim()` 真正提交非零力时直接崩：

```
[Error] [omni.physx.tensors.plugin] CUDA error: an illegal memory access was
        encountered: .../gpu/GpuRigidBodyView.cpp: 810
RuntimeError: copy_if failed on 2nd step: cudaErrorIllegalAddress
```

有时报在 `CudaKernels.cu:1498` / `Failed to submit rigid body forces`，
是同一个东西的不同暴露点。

**原因**：**服务器有 8 张卡，启动时没有 pin `CUDA_VISIBLE_DEVICES`。**
Isaac Sim 启动日志里 8 张卡全是 `Active: Yes`，PhysX 的 GPU 管线和
tensor API 的缓冲不在同一个设备上，提交外力时越界。

**注意 `SimulationCfg(device="cuda:0")` 挡不住**——那只设 Isaac Lab 侧的
torch 设备，不改 PhysX 选卡。

**解法**：所有 Isaac 脚本一律 pin 单卡：

```bash
CUDA_VISIBLE_DEVICES=0 PYTHONPATH=src /isaac-sim/python.sh tools/xxx.py
```

`tools/run_remote.sh` 已内置（`IT_GPU`，默认 0）。顺带好处：8 张卡可以
同时跑 8 个互不干扰的实验，`IT_GPU=3 ./tools/run_remote.sh ...`。

**实测对照（13 次运行，单变量）**：

| 条件 | 传感器 | track_pose | 板数 | 结果 |
|---|---|---|---|---|
| 8 张全可见 | 有 / 无 | 开 / 关 | 1 / 2 | **全部崩** |
| `CUDA_VISIBLE_DEVICES=0` | 有 | 开 | 2 | 通过 |
| `=1` / `=2` / `=4` | 有/无 | 开/关 | 1/2 | 通过 |

**同一张物理 GPU0，不 pin 就崩、pin 上就过**——所以不是坏卡。

**未解释的残留**：`tools/s2_scripted.py` 在 8 张卡全可见时**不崩**，
S2 全程就是这么跑的。所以「多卡可见」是必要条件不是充分条件，
还有一个与 `DirectRLEnv` 相关的因素没定位。**但 pin 卡对两者都有效，
不再往下追**。若将来又冒出同类崩溃，先确认这条。

**代价与教训**：

这个坑烧掉了一整轮工作。另一个 agent 在它上面连试十几个变体、
改废了 `s3_source_drawer.py`，全程在改**本来就没错的代码**。

1. **崩溃不一定在代码里。** 换一台机器/换张卡/换个启动参数能过，
   就说明不是代码。这个对照应该**第一个**做，不是最后一个。
2. **`torch.cuda.synchronize()` 抓不到 PhysX 内部的 CUDA 错误。**
   分阶段打 OK 会给出**假的**定位——前 8 个 stage 全 OK，
   实际问题在别处。P-27 说「别猜，去测」，但测的方法本身也得先验证。
3. `CUDA_LAUNCH_BLOCKING=1` 在这里既没帮上忙也没添乱，与崩溃无关。

---
## P-30 · 逐点接触数据的 env 归属是错的，而且会整段丢失 ⭐⭐

**日期**：2026-08-29（S3 采集）

**现象**：三个症状，一开始看着毫不相干。

1. env 3 记到的接触点，转进物体系后 y 偏了 **−2223 mm**——恰好一个
   `env_spacing`。env 0 完全正常。
2. 某些 episode 抽屉明明被拉开了 **97 mm**，接触点却**一个都没有**。
   板压进横杆 0.5 mm（逐帧位姿可查），力就是不报。
3. 归类表里凭空出现 58% 的 `other` 部位。

**原因**：`view.get_contact_data()` 返回的 `counts` / `start_idx` **两件事都不能信**。

- **下标不是 env 下标**。实测：只有 env 2 有接触时，`counts` 报
  `[0, 0, 0, 2]`、`start_idx` 报 `[0, 0, 0, 0]`——数量和位置都对，
  但**记在了 env 3 名下**。于是 env 3 拿到 env 2 的接触点。
- **总数会漏报**。按前缀和圈出的"新鲜槽位"覆盖不到全部真实接触点，
  落在覆盖范围外的点被整段忽略——症状 2 就是这么来的。

**为什么静态自检发现不了**：所有 env 同时有接触时，下标恰好是恒等的。
我先后写了两个静态对拍脚本（单板 / 双板 + 双传感器），**全部通过**。
只有在"接触逐个 env 先后建立"的真实采集里才会错开。

**解法**（`it.contact_utils.extract_contact_points_padded`）：

```text
counts/start_idx 只用来……什么都不用。
1) 扫全 buffer，取 |法向力| > 阈值的槽位
2) env 归属按"这个点离哪个 env 的传感器本体最近"判
3) 离所有本体都超过 own_radius 的点丢弃并计数（foreign）
```

修复前后（同一批 40 个 episode，同一 seed）：

| | 保留的接触点 | 成功率 | 接触落在 bar_back |
|---|---:|---:|---:|
| 前缀和切片 | 5730 | 52.5% | 52% |
| 扫全 buffer + 按位置归属 | **13786** | **77.5%** | **68%** |

**这条推翻了 P-18 的做法。** `extract_contact_points`（逐 env 循环版）
用的正是前缀和切片，它的数值本身没错，但 env 归属只在"所有 env 同时接触"
时才对。该函数因为是 S1/S2 的既有依赖而保留，docstring 里已加警告。

⚠️ **`decisions.md` D-34（钩杆接触部位分布）是用旧方法算的。** 当时 16 个 env
几乎同时接触，下标大概率是恒等的，但**没有验证过**。引用那组数字前应该重算。

**教训**：静态自检通过不等于对。这个坑的三个症状里，只有第 1 个像 bug，
第 2 个（接触整段消失）看起来像"物理没接触上"，第 3 个像"分类器阈值不对"——
我为后两个各写了一轮错误的修复。**把三个症状放在一起看，才看出是同一个根因。**

---

## P-31 · 一个控制步只在末尾采一次接触，会漏掉大部分接触

**日期**：2026-08-29（S3 采集）

**现象**：板与横杆的间隙逐帧稳定在 −0.5 mm（实实在在压着），抽屉被匀速拉开，
但按控制步采样的接触**有一半帧是空的**，`substep_contact_fraction` 只有 0.13。

**原因**：PhysX 只在**部分物理子步**里报告某个接触对。控制频率 50 Hz、
物理 300 Hz，一个控制步有 6 个子步；只读最后一个子步，就只能看到大约
1/6 的报告。接触是连续的，**报告不是**。

**解法**：每个物理子步都取一次，保留**法向力最大**的那一子步作为该帧的代表。
不是取最后一个，也不是求平均——平均会把"这一帧接触多大"稀释成"报告了几次"。

记录频率仍是 `plan/03` §5 规定的 50 Hz，改的只是"这一帧拿哪个子步代表"。

---

## P-32 · 两个采集体生成在同一位置，互相穿插后一起卡死

**日期**：2026-08-29（S3 采集）

**现象**：`pinch` 两个家族成功率只有 2~5/8，而 `hook`/`asym` 是 8/8。
失败的 episode 里两块板**一起卡在横杆底下**，接触力 74 N，
接触部位 100% 判成 `bar_bottom`，板被压到比目标低 23 mm。

**原因**：两块板的**起始位相同**。pinch 家族两块板的 y 只差几毫米，
而板是 35×25 mm——生成时它们几乎完全重叠。PhysX 把互相穿插的两个刚体
粘在一起，approach 阶段走不到位（实测只走了 79%），于是升起来的时候
不在净空里而在横杆正下方，一升就顶住，再被自己的位置 PD 死死压住。

`hook`/`asym` 两块板 y 相距 ≥38 mm，从一开始就没这个问题——
**成功率按家族劈成 8/8 和 2/8 两档，这个分布本身就是线索**。

**解法**：起始位在 X 上错开 80 mm（`HOME_STAGGER`）。改完立刻从 32/40 变成 39/40。

**教训**：**先看指标是不是按某个分组劈开的**。我在这之前花了三轮去调
接触阻尼、力控增益、拇指控制方式——都是在改一个本来没错的东西，
而分组差异一眼就指向"两个家族的参数有什么结构性不同"。

---

## P-33 · 力控轴的速度阻尼是相对世界的，物体一动就把接触力扣光

**日期**：2026-08-29（S3 采集）

**现象**：两种表现，看着完全无关：

- 指令力小的家族（每块板 2~3 N）**跟不上抽屉**，接触断断续续；
- 反向推的那块板（拇指，指令 −X 而抽屉往 +X 跑）**一接触就分离**，
  之后只剩阻尼、以 0.27 m/s 一路飘进柜体撞面板，把平均接触力顶到 30 N。

**原因**：`FloatingPD` 力控轴的输出是 `ff_force − kd_force·m·v_world`。
阻尼是**相对世界**的，而板必须跟着被操作物体走。抽屉以 0.2 m/s 打开时，
`kd_force=35`（→17.5 N·s/m）白扣 **2.3 N**，比某些家族的指令力还大。

**解法**：把 `kd_force·m·v_object` 前馈回去，等价于"阻尼相对被操作物体"：

```python
ff[:, 0] = -side * force_mag * engage + pd.kd_force * pd.mass * drawer_v
```

于是稳态接触力精确等于指令力，与物体开多快无关。
同一条思路对任何"跟着运动物体施力"的场景都适用，S4 之后的执行器同样要注意。

**顺带**：位置控制跟运动目标也有同一类问题——稳态滞后 `e = kd·v/kp`，
抽屉 0.2 m/s 时是 7.3 mm，乘 1500 N/m 的位置刚度就是 11 N 的"刹车力"。
所以不要用位置 PD 去跟一个在动的接触面，除非把滞后前馈补掉。

---

## P-34 · 薄板的姿态 PD 在 150 Hz 下数值不稳定，无接触时也偏 20°

**日期**：2026-08-29（S3 采集）

**现象**：采集板在**自由飞行、完全没有接触**的 approach 阶段，
姿态误差就稳定在 19~21°，峰值 74°。

**原因**：显式积分的姿态 PD 稳定条件是 `ω·dt < 2`，`ω = sqrt(kp_rot·m/I)`。
板 35×25×3 mm、0.05 kg，绕长边的 `I = m(b²+c²)/12 ≈ 2.6e-6 kg·m²`——
比钩杆小**两个量级**。本仓库其他地方的默认值 `kp_rot=50` 在 150 Hz 下给出
`ω·dt = 5.4`，直接发散成一个 20° 幅度的极限环。

**解法**（两条一起用）：

1. 采集用的物理步长 150 Hz → **300 Hz**（记录仍 50 Hz，decimation 6）。
   dt 减半，允许的刚度变成 4 倍。
2. 板质量 0.05 → **0.5 kg**。惯量正比于质量，这是不改尺寸的唯一办法。
   物理上也说得通——采集面背后本来就该有一只手或一段器械。

之后 `kp_rot=6`（`ω·dt = 1.2`），实测姿态误差 5.5°，峰值 12°。

**教训**：增益不能跨执行器照搬。**先算 `ω·dt`，再决定用不用。**

---

## P-35 · 用"离哪个面近"判接触落在薄板哪一面，一半会判错

**日期**：2026-08-29（S3 采集）

**现象**：验收表里凭空出现 **36% 的"拿边角蹭"**（`edge`），
而录像里板明明是拿正面压着横杆。

**原因**：板只有 3 mm 厚（半厚 1.5 mm），而 PhysX 报的接触点在两个表面之间
浮动——实测一次正常的**工作面**接触，点的局部 z 在 **0.3~1.7 mm** 之间摆。
用"离哪个面近"判，阈值放在哪儿都会把一半正常接触判成侧边。

**解法**：**按接触法向判，不按位置判**。实测 100% 的面接触满足
`|n_z| > 0.7`，且法向在板局部系里恒指向工作面朝外的方向（+Z）；
真正的棱边接触，法向躺在板平面内，`|n_z|` 明显小于 1。

改完 `edge` 从 36% 掉到 0%，`work_face` 100%。

**教训**：分类器出来的异常，先确认是**物理异常**还是**判据异常**。
这一条差点让我去"修"一个根本不存在的物理问题。

---

## P-36 · 摩擦 buffer 有自己的下标，拿接触的下标去索引它，一半环境翻倍、一半全零 ⭐⭐

**日期**：2026-08-29（S3 收尾复跑 `check_contact_sensor.py` 时发现）

**现象**：`check_contact_sensor.py` 报出来的摩擦力**恰好是理论值的 2 倍**：

```
理论阈值 mu*m*g = 1.962 N
  施加 1.0 N -> 摩擦 1.9999 N   （应当是 1.000）
  施加 2.5 N -> 摩擦 3.9240 N   （应当饱和在 1.962）
```

**"恰好 2 倍"很容易被当成单位问题或 P-18 的跨环境求和**，我一开始就是这么以为的，
先去改了求和方式——没用，数字一模一样。

**原因**：`get_contact_data()` 和 `get_friction_data()` **各有一套 counts/start_idx，
行数还不一样**。同一帧实测：

```
接触 counts = [4, 4]   start_idx = [0, 4]
摩擦 counts = [2, 2]   start_idx = [0, 2]
```

PhysX 的摩擦锚点是按**接触斑块**给的（一个斑块通常 2 个锚点），与接触点不是
一一对应。拿接触的下标去切摩擦 buffer：

- env0 读 `[0:4]` → 拿到自己的 2 个锚点 **加上 env1 的 2 个** → 恰好 2 倍；
- env1 读 `[4:8]` → 全是零 → **这个 env 完全没有摩擦力**。

**后果范围**：`plan/02` §3.4 的 Contact Mode 判据是 `|f_t| < 0.95·μ·|f_n|`，
摩擦翻倍会把本该 sticking 的接触判成 sliding。S3 抽屉数据集第一版的
`stick_share` 是 0.72，修复后是 0.36——**mode 标签整体是错的**，数据集已重采。

**解法**：摩擦按**它自己的** counts/start_idx 取（或者同 P-30 一样扫全 buffer
按位置归属），再把斑块级的摩擦按**法向力占比**摊到各接触点上——
那是库仑摩擦的标准假设，摊派后各点之和精确等于斑块实际摩擦力，
且每点的 `|f_t|/(μ·f_n)` 仍然是判 stick/slide 的正确依据。
见 `it.contact_utils._friction_into`。

修复后与理论逐项吻合：

```
  施加 1.0 N -> 0.9999   施加 1.5 N -> 1.5003
  施加 2.5 N -> 1.9620   施加 3.0 N -> 1.9620   （= mu*m*g，饱和）
```

**两条教训**：

1. **`check_contact_sensor.py` 是"改动接触相关代码后必跑"的脚本，而它自己
   坏了很久没人发现**——因为它把结果写进 `/tmp/verify3.txt` 而不是 stdout，
   又在结尾调 `app.close()`（P-19 会挂起），所以从外面看它永远"还在跑"。
   **自检脚本本身也要能被看见结果、能正常退出。** 两条都已修。
2. `log/progress.md` 里 R-4 当初记的 1.000 / 1.962 是**对的**，是后来搬进
   `tools/` 的版本改坏了。**归档结论与现行代码对不上时，先怀疑代码。**

---

## P-37 · PhysX 接触法向的正负约定，换个场景就会翻

**日期**：2026-08-29（探针物体集采集）

**现象**：`classify_plate_face` 用接触法向在板局部系的 z 分量判"接触落在
工作面还是背面"。抽屉场景里 100% 判成工作面（与录像一致）；同一份代码搬到
探针方块上，**100% 判成背面**——而板明明是拿正面压着方块。

**原因**：接触法向指向哪一侧取决于这一对刚体在 PhysX 内部的先后次序，
**不是**"从传感器所在的体指向对方"这种稳定约定。抽屉里是 +Z，方块里是 −Z。

**第一次修错了**：改成按接触点在板局部系的 z 的正负判。方向是对的
（几何量没有约定问题），但**逐帧**判仍然会翻：板半厚只有 1.5 mm，
接触力 1 N 左右时那个量在零附近抖，同一次贴合被判得一半正面一半背面，
实测 push 这类原语的"工作面占比"只有 37%。

**最终解法（两步，都不依赖法向正负）**：

1. **面还是棱**：看 `|n_z|`。这一步只用法向的**大小**，与正负无关，很稳
   （实测正常面接触 `|n_z| = 0.9985`）。
2. **哪一面**：看接触点的加权 z 均值，且**在整条 episode 上判一次**，不逐帧判。
   脚本化的板在一条 episode 里只会贴住同一个面。

**更重要的一条结论**：**"正面还是背面"在轻接触下根本测不出来，
而且脚本化的板从不掉头，"用背面接触"在几何上到不了。** 所以验收判据
最终只用第 1 步（面还是棱）——那才是真正要防的"拿边角在蹭"（D-34 那类问题）。
测一个到不了的东西没有意义。

---

## P-38 · 运行时写关节阻尼会盖过资产里的值，改了资产等于没改

**日期**：2026-08-29（探针物体集采集）

**现象**：把翻板的关节阻尼从 0.05 提到 0.7、再提到 2.0，重建资产、重跑，
**结果逐位相同**——crank 成功 3/9、脱手 39.3%，一个小数点都没变。

**原因**：采集器为了做 `plan/03` §7 的 `unseen_physics_test`，每批都会
`write_joint_damping_to_sim()` 写一个采样出来的阻尼。那个值来自采集器里的
`ObjectSpec.damping_nominal`，**运行时覆盖资产和 actuator 配置里的一切**。
改 `build_assets.py` 和 `assets.py` 都没用，因为它们在第一帧就被盖掉了。

**解法**：改物理参数时，先查这个参数**在运行时有没有被写过**。
`ObjectSpec.damping_nominal` 处已加醒目注释。

**教训**：**"结果逐位相同"是一个极强的信号，说明改动根本没生效。**
物理量改了而结果一位不差，在有随机数的仿真里不可能是巧合。
遇到这种情况先怀疑改动没到，不要去解释"为什么这个改动没有效果"——
我第一反应是"大概阻尼不是瓶颈"，又白花了一轮。

---

## P-39 · 力控的轴掩码是世界轴对齐的，法向不与世界轴对齐时不能用

**日期**：2026-08-29（探针物体集采集）

**现象**："捏住物体挪走"这条原语的物体位移**恒等于 0.0 mm**，
接触力、姿态、接触位点全部正常。

**原因**：`FloatingPD` 的 `force_mask` 是**逐世界轴**的开关。抽屉那边接触法向
恰好是世界 X，设 `mask[:, 0] = True` 正好；探针物体的接触面法向千奇百怪，
我图省事写成 `mask = (engage > 0).expand(n, 3)` ——**三个轴全成了力控**，
于是切向的位置指令完全不起作用，扫掠了个寂寞。

**解法**：给 `FloatingPD.compute` 加 `force_dir`，做**沿任意方向**的混合控制：

```python
f_tan = f_pd - (f_pd·n) n          # 正交补里仍然位置 PD
f_nrm = ((ff·n) + (grav·n) - kd_force·m (v·n)) n
f = f_tan + f_nrm
```

**顺带一条**：判"物体本来就不该动"时，对**关节物体**必须查关节角。
自由体的 `root_pose` 变了才算动，而一扇被推开 100° 的门，
它的 body pose 在关节坐标下是常量——只查位姿会把它判成"静止"。

---

## P-40 · 阶段切换时目标位置跳变，把被操作物/工具甩出去 ⭐

**日期**：2026-08-29（S3 擦拭与旋钮采集）

**现象**：擦拭时黑板擦从夹持中滑脱，滑脱量近 **一米**；而且失败**只发生在
蛇形趟数为奇数的家族上**，趟数为偶数的家族毫发无损。

**原因**：`release` 阶段用 `u=0` 取路径点，也就是**回到路径起点**。
蛇形趟数为偶数时终点恰好等于起点，跳变为零；奇数时终点与起点相隔 282 mm，
位置 PD 收到这个阶跃直接输出上百牛，把工具甩出去。

同一类问题在栅格路径的**换道**处也出现过：X 按趟数跳变 65 mm，
瞬间 1500 N/m × 0.065 = 97 N，工具漂移 1.9~2.7 米。

**解法**：**任何阶段切换处，目标位置必须连续。**

- `release` 从路径**终点**继续，不是回起点；
- 换道用连续漂移，不用阶梯；
- 需要"退出"时，先在原地把夹持松开，再移动。

**教训（这条比现象本身重要）**：**失败率按某个参数劈成两半时，
那个分布本身就是线索。** 趟数偶数全过、奇数全挂——这不可能是"夹持力不够"
（我第一反应是加夹持力，从 2.5 mm 加到 4.5 mm，结果更糟：下压力被顶到 8 N）。
与 P-32（成功率按家族劈成 8/8 和 2/8）是同一个教训，我又犯了一次。

---

## P-41 · "法向力在允许区间内"是**逐接触体**判的，不能按总力分摊

**日期**：2026-08-29（S3 擦拭采集）

**现象**：直擦家族（两块板直接擦平面）的清除率卡在 **26%**，
而持工具家族都在 70~90%。两者的下压力指令一样。

**原因**：`plan/01` §5.3 的清除判据是"**任一接触体**的法向力位于任务允许范围
（3~8 N）"。持工具时接触体只有一个（黑板擦），指令的 5 N 全落在它身上；
直擦时接触体是**两块板**，5 N 一分为二，每块只有 2.5 N，**低于 3 N 下限**，
于是一格都擦不掉——动作完全正确，判据一格不给。

**解法**：把"下压力"的语义定死为**每个接触体的力**，而不是总力。
持工具：δ = press / (2 × 板刚度)；直擦：δ = press / 板刚度。
改完直擦清除率从 26% 升到 75%。

**教训**：判据里出现"任一接触体"这种量词时，要回头确认**指令侧**是不是也
按同一个粒度给的。这类不一致不会报错，只会让某一档数据莫名其妙地差。

---

## P-42 · 伺服一个转动物体时，参考系必须挂在**接触体的实际位置**上 ⭐

**日期**：2026-08-29（S3 旋钮采集）

**现象**：推销钉转圆盘。板"跟着圆盘走、领先一个固定角度"，领先角由目标角速度
反推。结果接触力被顶到 **49.9 N 纹丝不动**（PD 饱和），圆盘停在 1.709 rad
转不动，净力矩只有 0.015 N·m。

**原因**：板的位置是这样算的——先沿弧提前 `r·lead`，再沿该处切向偏
`pin_r + t/2`。**两个偏移叠加，板跑到销钉的斜前方**。量出来那一帧的接触法向
指向 +141°，而销钉在 98°、切向在 188°：**力有一大半是径向的**，顶着销钉往
圆心推，不产生任何力矩。为了补偿又要更大的力 —— 正反馈。

```
领先角 rad   法向偏离切向   力矩有效分量
   0.05          10.6°         0.98
   0.10          21.0°         0.93
   0.20          42.1°         0.74     <- 三分之一的力白费
```

**解法**：参考系挂在**销钉当前实际角度**上（`arc(theta=joint_pos)`），板只沿
该处的切向偏移，法向恒为纯切向；推进量改由**力控**给，不由领先角给。

**教训**：伺服一个运动目标时，"提前量"和"贴合偏移"必须在**同一个参考系**里
表达。放在两个不同的坐标里叠加，方向误差会随提前量增长，而增益补偿会放大它。

---

## P-43 · 单向关节的物体，别把"转向"做成随机 ±1

**日期**：2026-08-29（S3 旋钮采集）

**现象**：两次控制方案完全不同的实验，家族的平均转角却几乎一模一样——
0.780 vs 0.782、0.814 vs 0.813、1.140 vs 1.142。改什么都不动。

**原因**：旋钮的 revolute 关节限位是 **−10°~+200°**（`KnobCfg.joint_limit_deg`），
是个带死挡的单向旋钮。而我把转向采成随机 ±1，于是**一半 episode 一上来就顶在
−10° 死挡上**，目标角 1.0~2.2 rad 在那个方向根本不存在。平均转角里一半是
0.17（死挡）、一半是真实值，所以对控制不敏感。

**解法**：`turn_dir = +1`。修完"没转到目标角"从 58 条掉到 10 条。

**教训**：`P-38` 说"结果一模一样 = 改动没生效"，这里是它的变体——
**结果对控制不敏感 = 有一半样本根本不受控制影响**。看到"改什么都不动"就要去
查约束（关节限位、行程、死挡），而不是继续调控制。

---

## P-44 · 光滑柱面上不能用纯力控压：没有位置反馈拦着板往下钻

**日期**：2026-08-29（S3 旋钮采集）

**现象**：`rim_only` 两块板径向压圆盘轮缘，接触力量到 **603 N**、峰值 296 N，
而力矩为零。

**原因**：轮缘是 Ø140 的光滑柱面（μ=0.10），可贴合的边带只有 15 mm 厚。
径向走力控时那个轴**没有任何位置反馈**：板压进 8 mm 之后顺着边沿滑下去，
钻到圆盘底下，卡在立柱上，位置 −20 mm、1229 N 不动。

**解法**：轮缘家族改回**位置干涉**（自限的：r 和 z 都被位置 PD 按住），
干涉量按实测力定（12 mm → 32 N 超上限，6.5 mm → 19.5 N）。销钉家族保留力控。

**教训**：力控只在**接触面能提供侧向约束**时才安全。压在凸曲面、光滑面、
或接触面积小于工具面的地方时，力控轴要么配一个位置限位，要么就别用力控。

---

## P-45 · 力控不能从"零穿透"起步

**日期**：2026-08-29（S3 旋钮采集）

**现象**：板贴在销钉表面（指令干涉 0），操作阶段一开力控就**整块从 Ø20 的
销钉里穿过去**，撞在另一块板背上。接触点数**全程为 0**——因为板与板的接触
不在接触传感器的过滤名单里，所以连异常都看不见。

**原因**：零穿透时接触力是零，前馈力让板**自由加速**；`kd_force` 的终端速度
是 `F/(kd·m)`，一个控制步就走进去几毫米，接触还没建立起来就已经过去了。

**解法**：establish 相位先用**位置干涉**压出 0.8 mm 的实接触，再把该轴交给
力控。另外把 `kd_force` 调大，限制接近速度。

**教训**：混合力/位控的交接点要落在**已经接触**的状态上。还有一条：
接触传感器的过滤名单只列了被操作物体时，**工具之间的互相干涉是完全不可见的**
——`np=0` 配上"板被顶开"就是这个签名。

---

## P-46 · 推圆柱时，板的长边必须与圆柱轴平行（动态接触，不是几何摆错）

**日期**：2026-08-29（S3 旋钮采集）

**现象**：同一块 35×25×3 的板，竖放（35 mm 沿销钉轴）推得动，横过来
（25 mm 沿轴、35 mm 沿径向）就**径直穿过销钉**，接触点数为零。

**测量**（沿销钉高度扫 14 个位置，纯销钉段的平均接触力 N）：

```
高度 mm    20.6  24.8  28.9  33.1  37.2  41.4  45.5  49.7
横放        0.4   0.0   0.4   0.0   0.0   8.3   6.1   8.0
竖放       13.3   5.7   5.5   5.5   5.5   6.8   3.7   6.6
静态位控    2.7   3.3   3.3   2.9   2.6   3.3   1.8   0.0
```

**静态位置控制下两种朝向都正常**（2 mm 干涉都量到 3 N），所以这不是几何摆错，
是**动态接触**问题：横放时板在切向力控下会从圆柱面上滑脱。

**解法**：销钉家族一律竖放。唯一的例外是 `pin_push_dual`——两块板要沿销钉
上下错开，而销钉可用段只有 48 mm，放不下两块 35 mm 的竖板（需要 71 mm），
那一档只能横放，而横放对它可行（两块板上下夹住销钉互相约束）。

**教训**：怀疑"几何摆错"时，先用**静态位置控制**测一遍同样的几何。
静态正常、动态失效，说明要查的是接触的动力学条件（朝向、接触线长度、
力控轴的约束），不是坐标算错。

---

## P-47 · "操作阶段脱手率"不能把**任务已完成之后**的窗口算进去

**日期**：2026-08-29（S3 旋钮采集）

**现象**：同一批干净轨迹，把 manipulate 窗口从 130 步加到 155 步，
"脱手率"从 27% 涨到 38%，成功率从 39% 掉到 12%。驱动段本身一帧没变。

**原因**：转到目标角之后板就不再推、只轻轻把住，那段窗口里接触时有时无。
把它算进"脱手"，等于**窗口留得越宽裕、数据看起来越差**——指标在惩罚留余量。

**解法**：脱手率只在**驱动段**（`progress < 1`）上统计，并把保持段的接触率
单独列一栏，不藏。判据本身（>30% 判失败）一点没放宽。

**教训**：这不是"放宽判据"，是让指标测它本来要测的东西。分辨的办法是问：
**改完之后，真正的失效模式还会不会被抓到？** 驱动期间掉接触仍然照判；
而"任务已完成"从来就不是失效。

---

## P-48 · 板绕工作面法向的**滚转是个没人管的自由度**，标记因此互相矛盾

**日期**：2026-08-29（用户看录像时指出）

**现象**：用户看 demo 时问："为什么很多视频里两块板一上一下，像是反转了 180 度？"

量出来确实如此——把每块板的**局部 +Y**（深色鳍所在的边）的世界朝向取操作
阶段均值，两块板的方向余弦：

```
抽屉  五个家族                    +0.98 ~ +1.00   （本来就对）
擦拭  tool_* 五个家族             -1.00           P0 鳍朝下、P1 鳍朝上
旋钮  pin_pinch/single/regrasp    -0.99
探针  block/slider 的全部对捏原语  -1.00
探针  column pinch_turn / twist    -0.99
探针  block press / shear          0.00           不参与的那块板朝向另一个位点
```

**原因**：所有采集器都用 `quat_from_frame(z_axis, x_hint)` 摆板，它只钉住
**工作面法向（局部 +Z）**和**局部 +X**，**局部 +Y 是算出来的**（Y = Z × X）。
而深色鳍长在 +Y 那条边上。两块板一旦面对面（任何对捏/双面夹持），
两个 +Z 相反，+Y 就必然跟着相反 —— 滚转这个自由度从来没有人约束过。

另有一个独立的小问题：单位点原语里**不参与的那块板**的位点下标没赋值，
默认落在 `site_names[0]`（字母序第一个位点），于是它朝着另一个方向待命。

**这在物理上是恒等的**：板是 35×25×3 的长方体，绕自身任一主轴转 180° 与自身
重合，而标记只有视觉没有碰撞（`build_assets.PlateCfg`）。板的位姿又是
`source/*` 审计字段，被隔离在模型输入之外。所以**没有一条结论受影响**。

**但它让标记失去了唯一的用途。** 加这个标记就是为了让人在录像里读出板的姿态
（`plan/06` §7 的人工检查、以及用户在 S3 开始时明确要求的"给板子做个标志，
能看清方向"）。两块板的标记互相矛盾时，这个用途就没了。

**解法**：`contact_attrib.quat_face_and_up(z_axis, up_hint, long_axis=None)`
把滚转变成**显式指定**的量：

* 长边不受约束时 —— 取 `x_hint = up_hint × z`，于是 +Y ≈ up_hint（鳍朝上）；
* 长边被物理约束时（推销钉必须长边沿柱轴，见 P-46）—— 只在 ±long_axis 两个
  **等价解**里挑符号，让 +Y 与 up_hint 同向。轴不动，只定符号。

修完全部 ≥ +0.97。

**教训**：给一个物体"正对某个表面"定姿态时，只给法向是**不够**的——绕法向
还剩一个滚转自由度。它不影响物理（对称体），但会影响一切靠外观读姿态的东西。
把它写成显式参数，不要让它从叉乘里掉出来。

---

## P-49 · 控制回路里**每步重算**的目标，其离散选择必须算一次就固定 ⭐

**日期**：2026-08-29（修 P-48 时自己引入，A/B 对拍查出）

**现象**：为了修 P-48（两块板的朝向标记互相反 180°），我在摆板时加了一句
"+Y 与参考方向反了就把长边取反"。改完探针几个物体大幅退化：

```
立柱 pinch_turn   51/52 -> 16/52   夹持力 14.1 N -> 8.7 N   物体位移 226 -> 1046 mm
立柱 twist        51/52 -> 19/52   夹持力 13.3 N -> 6.6 N
转盘 整体         100%  -> 74%
```

**原因**：翻转判据写成 `flip = (y·up_hint < 0)`。而在**圆柱侧面**这类位点上，
板的 +Y 恰好水平，`y·up` 数学上正好是 0、实际是 ±1e-9 的浮点噪声。
姿态在控制回路里**每个控制步重算一次**，于是目标四元数在相邻步之间
**随机翻 180°**，转动 PD 每步都想把板拧半圈：夹持力腰斩、物体被甩出一米。

⚠️ 注意这里翻 180° 对**几何**是恒等的（长方体自映射），所以"改的是等价解"
这个判断没错——错在**它是在闭环里反复重新决定的**。

**解法**：`contact_attrib.roll_sign()`——符号在 episode 开始时算一次，
之后整条轨迹不再变；参考方向按优先级排（先"朝上"，|点积|<1e-3 就退到世界
+X、再退到 +Y），因此不存在"恰好正交所以由噪声决定"的情形。
修完 `pinch_turn` 52/52、`press` 49/51、`twist` 47/51，全部回到基线或更好。

**同一轮里的第二个**：单位点原语中不参与的那块板，我让它整个跟随第一块板，
结果 `press` 的待命板正好悬在工作板**正上方**，把它压成边角接触
（侧边接触 9.8% -> 62.5%，成功率 46/51 -> 8/51）。改成**位置留在原地、
只让朝向跟随**。

**教训**：

1. 控制回路里每步重算的量，如果内部含**离散选择**（符号、分支、argmax），
   那个选择必须在回路**外面**算一次。等价解之间反复横跳，在几何上是恒等的，
   在动力学上是灾难。
2. 判据里出现"某个点积的正负"时，先问**它会不会正好是零**。会的话就得有
   确定的退化处理，否则符号由浮点噪声决定。
3. 改一个纯视觉的东西也要 A/B 对拍。这次要不是拿旧代码同种子跑了一遍，
   只会看到"成功率掉了"，而根本想不到是那句翻转判据。

---

## P-50 · 只有 env 0 被录像时，家族分配必须让 env 0 轮换

**日期**：2026-08-29（用户看录像时第二次指出"有些还是没改过来"）

**现象**：修完 P-48/P-49 重采之后，用户审核发现擦拭的 `tool_tilt` 录像**还是老样子**。
查下来不是没改对，是**产物没更新**：擦拭 45 个批次只录到了 `tool_center`
一个家族，其余五个家族的 mp4 和接触表还是上一轮遗留在服务器 `/tmp` 里的旧文件，
被我用通配符 `scp /tmp/pw_*.png` 一起拷了回来。

**原因**：录像只录 env 0，而家族按 `fams[(b * envs + i) % len(fams)]` 分配。
`envs` 是家族数的整数倍时（60 个环境 / 6 个家族），`b * envs % len(fams)` 恒为 0，
**env 0 永远落在同一个家族上**。旋钮那边已经修过（改成 `(b + i) % len(fams)`），
但擦拭和抽屉没跟着改。

**解法**：三个采集器统一改成 `(b + i) % len(fams)`；每个批次仍然均匀覆盖所有家族，
而 env 0 逐批轮换，N 个批次就能拍到 N 个不同家族。

**两条更一般的**：

1. **拷产物不要用通配符。** 服务器 `/tmp` 里有上一轮的同名文件，通配符会把
   没重新生成的那些一起带回来，时间戳还是新的（scp 会更新 mtime），肉眼查不出。
   正确做法：**先清空目标目录和中间目录，再生成，再逐个显式拷贝**，
   最后核对"文件数对不对"和"有没有两个文件内容完全相同"。
2. **凡是靠肉眼验收的东西，都要有一个能重跑的核对脚本。** P-48 那次我是在临时
   heredoc 里量的一次性数字，没有固化，所以第二轮根本没人再查。现在做成
   `tools/s3_plate_orient.py`，四个任务 62 个家族/原语一次跑完，不达标非零退出。

---

## P-51 · 夹持的三个动作有严格先后：张开落下 → 夹拢 → 再下压

**日期**：2026-08-29（把擦拭的抓持面从短端面改成长侧面时暴露）

**现象一：直上直下落到夹持位，工具被顶飞。** 夹持位比工具本体窄
2×GRIP_INTERF，所以板是**拿下边缘楔在工具顶面上**落下去的。夹 45×25 的短端面
时工具要沿长轴挪 45 mm 才脱得出，勉强夹得住；改夹 90×25 的长侧面之后只要挪
22 mm——实测工具被顶高 23 mm、随即甩出 200 mm，六个持工具家族全线失败
（法向力 1.0 N、清除率 29%）。

**解法**：下落时夹持轴上先张开 6 mm，落到位再夹拢。

**现象二：改完之后工具夹住了（滑脱 0.2 mm），却一点压力都没有**
（清除率 0%、法向力 1.5 N）。

**原因**：张开着落下去时板不受阻，会**一路落到"已经下压"的那个高度**；
等夹拢时竖直方向的位置误差已经归零，而压力正是靠这个误差产生的
（板 z 实测−指令：+1.5 mm -> +0.15 mm，法向力 4.8 N -> 0.45 N）。

**解法**：把 establish 相位切成三段——张开落下 40%、夹拢 30%、加压 30%。

**教训**：位置控制里，"压力"是**位置误差**的产物。任何让工具先自由到达
目标位姿、再建立约束的顺序，都会把那个误差消掉，压力随之消失。
夹持类动作的三个量（张开量 / 夹持量 / 下压量）必须按物理顺序依次建立，
不能同时给。人拿黑板擦也是这个顺序：手指张开放下去、捏住、再往下按。

---

## P-52 · 采集板的角速度顶在 PhysX 上限，接触点相对速度整体不可信 ⭐⭐

**日期**：2026-08-29（S4 提取交互信息时发现）

**现象**：按 `plan/02` §3.4 用接触点的相对速度判 stick/slide，抽屉数据里
**98% 的接触被判成 sliding**。切向相对速度中位 70 mm/s、法向 350 mm/s，
而同一段里：

- `separation` 全程稳定在 **−0.644 mm**（接触根本没断，也没有在靠近或远离）；
- 接触点在物体系里的坐标全程 **(63.4, −36.9, 88.0) mm 一动没动**（0.1 mm 以内）。

把那个相对速度沿时间积出来是 **59 mm 的净滑移**。那 59 mm 没有发生。

**原因**：采集板的姿态 PD 处在数值极限环上，**角速度顶在 PhysX 默认的
100 rad/s 上限**——实测抽屉 96.9%、旋钮 100% 的操作帧上 |ω| ≥ 99.5 rad/s
（擦拭与探针集是 0%，那两个场景的板不转）。`contact_rel_vel` 里的
`ω × r` 项（力臂 17.6 mm）因此贡献 173 mm/s，压过了真实的相对运动。
`src/it/assets.py::_rigid_props` 设了 `max_linear_velocity` 却没设角速度上限，
所以顶的是 PhysX 的默认值。

**为什么很难发现**：板的**平均姿态**是好的（S3 实测姿态误差均值 5.6°、
峰值 12°），接触力、接触部位分布、成功率全部正常。坏掉的只有速度这一路，
而速度没有独立参照可比——除非像上面那样拿几何量（间隙、接触点坐标）去卡它。

**解法**：mode 改由**两个刚体的位姿差分**算相对速度（D-49）。位姿是位置量，
50 Hz 上差分等于对 6 个物理子步取平均，极限环自然抵消。
判据用擦拭数据集交叉验证过——那里板角速度是 0，三种估计逐帧相关 0.95、
各阈值下 sticking 占比差 0.5 个百分点以内。

⚠️ **初版的解法（接触斑块在物体表面上的位移）不够好，已被推翻**：它把
"平板贴圆柱时接触线随姿态微动而迁移"也算成滑移，抽屉上因此把 92.5% 的
sticking 判成一半在滑。见 D-49 的修订段。

**没有去动 S3 数据。** 位置量（region、接触部位、力）不受这个问题影响，
S3 的验收结论仍然成立；受影响的只有 `rel_vel_obj` 这一路，它在记录里
原样保留为 `mode/inst_slip`，并标了 `rel_vel_trusted`。

**下一轮采集时应当**：给板的 `RigidBodyPropertiesCfg` 显式设
`max_angular_velocity`（比如 20 rad/s），并把它当成一个**要监控的诊断量**——
角速度顶格是"姿态控制在极限环上"的直接信号，而姿态误差的均值看不出来。


---

## P-53 · 相对速度算在世界系，却拿物体系的法向去投影 ⭐

**日期**：2026-08-30（S4 提取器）

**现象**：无。逐帧数值全部正常，接触部位分布正常，动力学一致性正常，
单元测试全过——**没有任何征兆**。

**原因**：`interaction._pose_slip` 由两个刚体的位姿差分算出接触点相对速度，
那个速度是**世界系**的；紧接着要减掉法向分量取切向，用的却是**物体系**的
接触法向。两个不同坐标系的矢量做投影，切向分量整个是错的。
物体没转动时（擦拭平面）两个系恰好只差一个平移，投影仍然对——所以三个任务里
只有两个受影响，更难发现。

**怎么发现的**：`plan/02` §7 第 1 条的**场景刚体旋转检查**。把整个场景绕世界
原点转一个随机角度，物体系的表示按定义应当**逐元素不变**；实测滑移速度差到
**0.17 m/s**。修好之后是 1.05e-05 m/s（浮点噪声量级）。

**教训**：那条泄漏检查看起来像是防"表示里混进 source 信息"的形式主义，
**实际抓到的是一个纯粹的数值 bug**。物体中心表示里每一处坐标变换都应当被它覆盖。

**顺带**：这条检查自己也写错过两版——第一版忘了把接触体的世界位姿一起转
（mode 的主判据正是由那些位姿差分算的）；第二版把物体系的量也转了一遍，
等于把同一个旋转记了两次。**"整个场景刚体旋转"的正确写法是：所有世界系字段
转，物体系字段一个不动，输出逐位相同。**

---

## P-54 · 擦拭：接触体位姿是**世界坐标**，接触点是**平面系**，混用出 8 m 力臂 ⭐⭐

**日期**：2026-08-30（S4.6 首轮发现现象，同日查明根因）

**根因（已查明）**：`source/tool_pose` 记的是**世界坐标、含 env 原点偏移**
（env 间距 2.2 m，实测首帧 (6.342, −4.664, 0)），而 `contact/tool/pos_obj`
在**平面局部系**里。擦拭的平面是 kinematic 刚体、位姿**没有被记录**，
`interaction._object_pose` 只能假定它在原点——于是
``local = Rᵀ(pos_obj − tool_pos_world)`` 把两个坐标系的量相减，
**实测 |local| = 8000 mm**（正常应当是几十毫米）。

姿态噪声作用在 8 m 的力臂上，0.5° 就是 70 mm 量级——这就是那个 47 倍放大。

### ⚠️ 影响范围比"传感实验的数不好看"大

**同一条力臂也进了擦拭的 `mode/slip_speed`**：`_pose_slip` 里的 ``carried()``
用同样的方式算力臂。它没有当场暴露，是因为擦拭时工具的**姿态几乎不变**，
``(R[t+1] − R[t−1]) · local`` 这一项才小；**`tool_tilt` 那一族会更差**，
而且这件事从结果上看不出来。

**所以**：
- 擦拭的传感可观测性数字 **不引用**；
- 擦拭的 **mode 数字（sliding 86.9%）降级为"待复核"**——它与独立的瞬时速度
  判据交叉验证过（相关 0.95），大概率没错，但那次交叉验证用的诊断脚本
  **有同样的缺陷**，不能算独立；
- 抽屉与旋钮不受影响：它们的物体位姿是**记录了的**（`source/drawer_pos_w` /
  `source/disc_pos_w`），力臂正常。

### 解法（下一轮采集时做，几乎零成本）

`tools/s3_source_wipe.py` 把**平面的 root pose 记进 episode**（每条一列常量）。
这一条同时解掉三件事：P-54、`plan/02` §7 第 1 条在擦拭上 DEFER 掉的那条检查、
以及 `_object_pose` 里那个"假定它在原点"的写死分支。

**教训与 P-53 同源**：那次是相对速度算在世界系、法向在物体系；这次是接触体位姿
在世界系、接触点在物体系。**物体中心的表示里，每一个量都要标清楚它活在哪个系**——
两次都是零征兆，都要靠"换一个系再算一遍"才逮得到。

**现象**：`tools/s4_sensing.py` 在擦拭数据上，面位姿噪声 0.5 mm/0.5° 一档给出
**接触点偏移 23.43 mm**，1 mm 一档 46.86 mm——与噪声幅值成正比但放大约 **47 倍**。
抽屉与旋钮同一档只有 0.90 / 0.86 mm（放大不到 2 倍，符合预期）。
零噪声档三个任务都正常（0.57 / 0.72 / 0.82 mm）。

**为什么不能当结论用**：47 倍的放大没有物理来源。接触点由"面位姿 ∘ taxel 局部
位置"重建，误差的放大倍数最多是**力臂 / 噪声尺度**的量级；擦拭的接触点离接触体
原点只有几十毫米，放大到 47 倍不可能。**这是装置模型的 bug，不是可观测性结论。**

**已排除**：不是零噪声下的对齐问题（那一档 region 余弦 0.995、stick 一致 1.000，
说明重建路径本身是准的）。

**下一步查这三处**（按可疑度）：

1. `source/tool_pose` 的**约定**——擦拭的黑板擦用的是这个键，与板的
   `source/plateK/root_pose` 不是同一个命名，也未必是同一个位姿约定
   （是不是 7 维 wxyz、是不是同一个参考点）。约定错了在零噪声下会**完全抵消**，
   只有加噪时才暴露，正好符合现象。
2. **被操作物体的位姿被假定成原点**（见 D-51 与 `_object_pose`）。擦拭平面真实
   位置是 (0,0,−0.01)，而装置模型把噪声加在"相对这个假原点"的位姿上，
   等于把旋转噪声的力臂从"接触点到接触体原点"换成了"接触点到世界原点"。
   **这一条最可疑**，且与 P-53 同源：都是"物体系与世界系混用"。
3. taxel 量化用的是**真值**位姿、而重建用的是**带噪**位姿，两处的参考系要一致。

**在查清之前，擦拭的传感可观测性数字一律不引用。** 抽屉与旋钮那两份的放大倍数
正常，可以用，但同样要在报告里写明擦拭这一份待查——只报好看的那两份就是选择性
报告。
