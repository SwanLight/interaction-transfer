# 交接说明

**如果你是接手这个项目的新 agent 或新人，从这份文件开始读。**

本文件回答：这个项目在做什么、现在做到哪、下一步做什么、怎么在服务器上跑东西、
以及哪些坑会浪费你半天。

最后更新：2026-08-29

---

## 1. 一句话说清项目

不迁移人的动作，而迁移动作所实现的**功能性交互**，再由不同末端执行器
根据自身形态各自重新实现。

```text
双板采集器（脚本控制，模拟人采集）
        ↓  多样示教
提取交互信息（哪里接触 / 什么接触状态 / 需要什么机械作用 / 物体怎么变）
        ↓
不同执行器各自学「怎么实现这份交互」→ 各自的动作
```

**本轮只做仿真算法验证。** 真实触觉硬件、RGB-D、sim-to-real 都不在范围内。

主张是：**执行器是任务无关的**——在任务 A、B 上训练的执行器，
能对**留出任务 C** 零样本执行，不需要 C 的奖励、不需要微调。

---

## 2. 必读文档，按顺序

| 顺序 | 文件 | 为什么必读 |
|---|---|---|
| 1 | `plan/00-positioning.md` | 相对 KITE / ART-Glove / CHORD 的差异；**能声称什么、不能声称什么** |
| 2 | `plan/README.md` | 系统结构、两个执行器、五个信息条件、步骤与闸门 |
| 3 | `log/decisions.md` | **48 条技术决策及其被否决的备选**。改任何设计前先查这里，否则会重新提出已经被否决过的方案 |
| 4 | `log/pitfalls.md` | 51 条坑。**第 3 节「已知会静默失败的坑」尤其重要**，那些不报错、只让结论作废 |
| 5 | `log/progress.md` | 环境版本表 + 每一步的实测数据 |
| 6 | `plan/01`–`plan/07` | 具体设计 |

`idea/` 目录是**原始构想，已被 `plan/` 取代**，只作背景，不要照它执行。

---

## 3. 十二个步骤是什么（S0–S10）

前三步是**准备**，S7 才是要证明的东西。

| | 做什么 | 状态 |
|---|---|---|
| **S0** | 把画面录出来的能力（渲染管线、HTML 报告） | ✅ |
| **S1** | 资产做得对不对（尺寸、碰撞、摩擦、够不够得着） | ✅ |
| **S2** | 每个执行器物理上能不能完成任务 | 抽屉×钩杆 ✅，其余按需 |
| S3 | 双板采集器模拟人，产生多样示教 | ✅ 抽屉 / 旋钮 / 擦拭 / 探针集 15/15 / 多样性 |
| **S4** | **从示教里提取交互信息 ← 下一步** | ⬜ |
| S4.5 | 表示分辨率该定多少 | ⬜ |
| S5 | 从多条示教归纳出共享的交互要求 | ⬜ |
| S6 | 训练「实现任意交互规格」的执行器 | ⬜ |
| **S7** | **留出任务零样本 ← 本工作的主张** | ⬜ |
| S8 | 信息条件对照（哪类信息有用） | ⬜ |
| S9 | 扰动恢复 | ⬜ |
| S10 | 跨形态 + 反事实 | ⬜ |

### 产物放哪

**`out/` 一个 S 一个目录，按步骤编号**，后面 S3、S4 照此规则：

```
out/
  report.html      总报告，双击就能看（33 段视频内嵌，单文件 671 KB，覆盖 S0–S3）
  s0_pipeline/     可视化管线
  s1_assets/       资产与场景录像 + 35 项自检
  s2_expert/       训练好的策略录像
  s3_source/       抽屉双板 source 示教录像 + 接触部位分布 + 数据集验收
  s3_probe/        探针物体集：原语库覆盖核对 + 六条原语录像
  s3_wipe/         擦拭：dirt 覆盖图 + 三个家族录像 + 验收
  s3_knob/         旋钮：接触部位与受力方向核对 + 五个家族截图 + 验收
```

- **`out/`** = 产物，给别人看
- **`log/`** = 过程记录（进度、决策、踩坑、原始日志），给自己和接手的人看

有的东西两边都有：S1 自检表的原始输出在 `log/s1/`，报告里嵌了一份。

## 4. 现在做到哪

| 步骤 | 状态 | 说明 |
|---|---|---|
| S0 可视化 | ✅ 通过 | 6 段录像 + 自包含 HTML 报告。WebRTC 已取消（D-28） |
| S1 资产自检 | ✅ 通过 | 33 项 PASS / 0 FAIL；预训练物体集另加 12 项（D-39） |
| S2 Expert | ✅ 收尾 | 抽屉×钩杆，固定 43.1% / 随机 49.3%，Gate A 按 D-32 通过 |
| **S3 Source** | ✅ **完成** | 抽屉 ✅ 探针集 ✅（15/15 格）擦拭 ✅ **旋钮 ✅** 多样性 ✅ |
| S4 及之后 | ⬜ 未开始 | **下一步从这里开始** |

### S3 的当前状态（接手时先看这里）

**已完成 —— 抽屉 × 双板，800 条示教**

```bash
./tools/sync.sh
./tools/run_remote.sh "PYTHONPATH=src /isaac-sim/python.sh \
    tools/s3_source_drawer.py --envs 80 --batches 10 --out /tmp/s3_drawer_full" s3full
ssh root@10.0.6.98 'cd /workspace/interaction_transfer && PYTHONPATH=src \
    /isaac-sim/python.sh tools/s3_verify_dataset.py /tmp/s3_drawer_full --sample 60'
```

- 五个策略家族，739/800 成功（92.4%）
- **接触 90.2% 落在把手横杆背面、9.8% 在正面（拇指）**，
  横杆上下表面 / 支撑柱 / 面板全为 0；**100% 落在板的工作面**；
  **100% 落在两根支撑柱之间**
- 峰值单点接触力 11.55 N（上限 25），脏帧 0.00%
- 独立验收 15 项全通过（`tools/s3_verify_dataset.py`）
- 录像与摘要在 `out/s3_source/`，五个家族各一条

数据集在服务器 `/tmp/s3_drawer_full`（36 MB），按 D-23 不进版本控制。

**已完成 —— 探针物体集（交互原语库），10740 条 / 9250 成功（86.1%）**

⚠️ **这一块的设计依据在 2026-08-29 换过一次，接手前必须知道为什么。**

原先（D-39）是**照着留出任务需要什么反推物体**：擦拭要"带法向力持续滑移"
就加擦板，旋钮要"绕固定轴转动"就加转盘。用户指出那样"搞得像在摸索训练阶段
执行器就已经掌握了同一个任务"——**这不是误解，是真实的逻辑漏洞**：
物体照着留出任务反推，执行器在"摸索能力"阶段学到的就是留出任务本身。

现在（D-41）改为从一套**独立于本项目三个任务的交互分类学**推出物体集，
判据是**张满分类学**，不是"覆盖留出任务"。外部依据是 Huang 的接触模式枚举、
Bullock/Ma/Dollar 的手中心分类、Lynch & Mason 的非抓握原语谱系。
另加硬规则：**每条原语至少两个几何不同的物体承载**。

```bash
PYTHONPATH=src /isaac-sim/python.sh tools/s3_source_probe.py \
    --object block --envs 60 --batches 34 --out /tmp/s3_probe/block
PYTHONPATH=src /isaac-sim/python.sh tools/s3_coverage.py /tmp/s3_probe
```

10 个物体、15 条原语，**15 条全部**同时满足"≥200 条成功"与"≥2 个承载物体"。
上一轮是 10/15，五格是这样补上的（D-47）：roll 那一格此前只有卧柱在扛，
**把球真正采进来**就通了（球 210/210，卧柱 189/810）；twist 也加了球；slide_push 加了立柱；
pinch_hold / pinch_move 加了滑轨块挡片的两个窄侧面。
**分类学一个格都没删**（D-41）——试过而不成立的（自由卧柱的端面对捏）
也写在采集器的注释里，免得下一个人重试。

**已完成 —— 擦拭 source（主任务），2700 条 / 2606 成功（96.5%）**

`plan/01` §5.1 的两种实现都做了（持工具擦 + 直擦），六个策略家族，
**每个家族都有 ≥250 条成功轨迹**（`plan/03` §6 的门槛）。
**`direct_wipe` 381 条成功那一档不可省**——`plan/02` §7 第 8 条那条泄漏检查
（envelope 与"是否用工具"无关）只有它能提供数据。

```bash
PYTHONPATH=src /isaac-sim/python.sh tools/s3_source_wipe.py \
    --envs 60 --batches 45 --out /tmp/s3_wipe
PYTHONPATH=src /isaac-sim/python.sh tools/s3_dirt_map.py /tmp/s3_wipe --out dirt.png
```

⚠️ **验收擦拭必须同时看 dirt 覆盖图。** 录像看得到动作、**看不到污渍**，
而擦拭的 effect 只有 dirt 变化（`plan/02` §3.1 经 D-42 修订）——
"动作看着对但区域没擦干净"在录像里分不出来。

**已完成 —— 多样性验收（`plan/03` §4）**

抽屉 0.742 / 随机 0.200（3.7×），擦拭 0.996 / 随机 0.167（6.0×）。
`tools/s3_diversity.py`。混淆矩阵有结构：站位相同、只有力分配不同的家族互相混淆，
而 `single_finger` 与 `direct_wipe` 被完美分开。
⚠️ 这只证明**动作层面不同**；`plan/02` §7 第 4 条要的是从 envelope 预测策略
**显著更难**，要等 S4/S5 有 envelope 才能做，届时两个数并排报。

**已完成 —— 旋钮 source，2400 条 / 1693 成功（70.5%）**

四个销钉家族分别 480 / 297 / 480 / 448 条成功，`rim_only` 480 条**按设计全部失败**。
独立验收 15 项全通过，策略分类器 1.000（随机 0.250）。

```bash
IT_GPU=0 ./tools/run_remote.sh "PYTHONPATH=src /isaac-sim/python.sh \
    tools/s3_source_knob.py --envs 60 --batches 40 --video --out /tmp/s3_knob" knob
PYTHONPATH=src /isaac-sim/python.sh tools/s3_knob_contact.py /tmp/s3_knob
```

⚠️ **验收旋钮必须同时看 `tools/s3_knob_contact.py` 的输出。** 录像看得出板在
推销钉，**看不出力打在销钉的哪一侧、方向偏了多少**——而旋钮的全部力学就在
这两件事上。实测：力 **100% 打在销钉柱面**、**100% 打在推进方向的后侧**、
接触法向只偏离圆盘切向 **2.4~11.6°**（P-42 那个错版本偏 42°）。

**D-14 的操作级证据（本块的主要产出）**：`rim_only` 在压足 19.5 N
（安全上限 25 N 之内）时只转到 0.019 rad，销钉家族转到 2.34~2.76 rad。
τ_rim = 0.10×19.5×0.070 = 0.137 N·m ≪ τ_need = 0.28×1.5 = 0.42 N·m。

**销钉是竖直插在盘面上的拨杆，不是握住拧的旋钮**，所以推力方向必须随圆盘转：
参考系挂在**销钉当前的实际角度**上，力控方向取该处切向，每个控制步重算。
三条关键决定与它们各自的实测依据见 D-46 与 P-42/P-44/P-45/P-46。

### ⚠️ 接手前必须知道的一件事

**`decisions.md` D-34（钩杆接触部位分布：主杆 78%、横钩 4.3%）是用一个
已被推翻的方法算的**，见 `pitfalls.md` P-30。那组数字与用户看录像的观察一致，
大概率没错，但**引用具体百分比之前应当用新方法重算**。

**还有一条**：`src/it/envs/base.py::contact_summary` 和
`extract_contact_points` 返回的**摩擦力是错的**（P-36：摩擦 buffer 有自己的
下标，混用会让一半环境翻倍、一半全零）。S1/S2 依赖它们所以没动，
但 **S4 之后不得再用那条路径取摩擦**，要用
`extract_contact_points_padded`。

## 5. 怎么在服务器上跑东西

服务器 `root@10.0.6.98`，8 × RTX 4090。免密登录已配好
（本地 `~/.ssh/id_rsa`，需要 `ssh-add --apple-use-keychain ~/.ssh/id_rsa`）。

### 标准流程

```bash
cd "/Users/swanny/Desktop/Interaction Transfer"

# 1) 同步代码到服务器（服务器无 rsync，走 tar over ssh）
./tools/sync.sh

# 2) 后台跑 + 轮询到结束（避免 ssh 前台超时）
./tools/run_remote.sh "PYTHONPATH=src /isaac-sim/python.sh tools/xxx.py" 标签

# 3) 看结果
ssh root@10.0.6.98 'cat /tmp/标签.log'
ssh root@10.0.6.98 'test -f /tmp/标签.done && cat /tmp/标签.done'   # 退出码
```

**必须用 `/isaac-sim/python.sh`**，不是系统 python。
系统 python 是 3.12，Isaac Sim 的是 3.11.13，两者不通。

⚠️ **训练进行中不要 `sync.sh`。** 正在跑的进程已经把模块加载进内存，同步本身
不影响它；但**一旦它崩溃重启，就会用上新代码**，前后不一致而你不会察觉。
非改不可时，记下改了什么、是否碰了物理或奖励——只改诊断字段是安全的，
改了奖励则该轮训练结果作废。

### 服务器上的关键路径

| 路径 | 内容 |
|---|---|
| `/workspace/interaction_transfer/` | 代码同步目标 |
| `/workspace/interaction_transfer/assets_gen/` | 生成的 6 个 USD 资产 |
| `/workspace/interaction_transfer/runs/` | 训练输出 |
| `/workspace/isaaclab/` | Isaac Lab（**已 git init 做快照**，见 D-22） |
| `/isaac-sim/` | Isaac Sim 5.1.0-rc.19 |
| `/mnt/isaacsim_assets/` | 内置资产（308 MB，由 `tools/fetch_assets.py` 拉取） |

---

## 6. 代码结构

```text
src/it/
  build_assets.py   从代码生成 6 个参数化 USD（禁止手工编辑 USD / 导 URDF，见 D-07）
  assets.py         Isaac Lab 资产配置，固化硬规则 7–11
  float_ctrl.py     浮动底座 PD 控制 + 混合力/位控
  contact_utils.py  逐接触点提取、stick/slide 判定、绕轴力矩重建
  contact_attrib.py 接触点归到"物体的哪个部位""执行器的哪个面"
  records.py        episode 数据契约（npz + manifest，前缀隔离，fail-closed）
  envs/
    base.py         增量动作接口 + 定长接触摘要
    drawer.py       抽屉任务环境

tools/
  sync.sh                 同步代码
  run_remote.sh           后台跑 + 轮询
  fetch_assets.py         拉 Isaac 内置资产
  check_contact_sensor.py 接触传感器验证（改接触相关代码后必跑）
  s0_record.py / s0_all.sh  录像
  make_report.py          生成自包含 HTML 报告
  s1_check.py / s1_all.sh   资产几何与动力学自检
  s2_scripted.py          **任务可行性验证 + reward 标定（开训前必跑）**
  s2_smoke.py             环境冒烟
  s2_train.py             PPO 训练
  s3_source_drawer.py     **抽屉双板 source 采集**（五个策略家族）
  s3_source_probe.py      **探针物体集采集**（原语 × 接触位点）
  s3_source_wipe.py       **擦拭采集**（持工具 + 直擦两种实现）
  s3_source_knob.py       **旋钮采集**（销钉切向力控 + 轮缘位置干涉，D-46）
  s3_knob_contact.py      旋钮的接触部位与受力方向核对——录像替代不了它
  s3_plate_orient.py      采集板朝向标记一致性核对（P-48/P-49），四个任务一次跑完
  s3_dirt_map.py          擦拭的 dirt 覆盖图——录像看不到污渍，这张图不可替代
  s3_diversity.py         多样性验收：从原始 source 动作分类策略家族
  s3_verify_dataset.py    数据集独立验收（划分/留出集/哈希/字段隔离）
  s3_coverage.py          交互原语库的张满与冗余核对，未张满时非零退出
  s3_check_padded.py      两种接触提取实现对拍。**静态通过不代表对**，见 P-30
```

---

## 7. 会浪费你半天的坑（全部实测踩过）

### 7.1 会**静默失败**的（不报错，只让结论作废）

| 坑 | 现象 | 解法 |
|---|---|---|
| **P-17** filter 目标不是刚体 | `net_forces_w` 照常工作，但接触位置/摩擦力全失效 | 不动的物体也要用 `kinematic_enabled=True` |
| **规则 9** 摩擦组合模式 | PhysX 默认 `average`，低摩擦区碰高摩擦执行器就失效 | 所有材质设 `friction_combine_mode="min"` |
| **P-21** 关节力矩指令残留 | `set_joint_effort_target` 会一直保持，物体被持续驱动，几何全错 | 换阶段时显式清零，且每步都清 |
| **P-11** region 可从 effect 推出 | 消融显示"没有增量价值"，实际是任务设计问题 | 跑可推导性探针 |
| **P-26** 开环转角对推力非单调 | 数字看着对，但对参数极敏感 | 引用任何数字前先扫一遍参数 |
| **P-30** 接触数据串环境/整段丢失 ⭐ | `counts`/`start_idx` 的下标不是 env 下标，总数还漏报。**静态自检一定通过** | 扫全 buffer + 按接触点离哪个本体最近来归属 |
| **P-31** 只在控制步末尾采接触 | 漏掉大半接触，看起来像"物理没接触上" | 逐物理子步采，取法向力最大的那一子步 |
| **P-32** 两个采集体生成在同一位置 | 互相穿插被 PhysX 粘住，接近走不到位后一起卡死 | 起始位错开；**先看指标是不是按某个分组劈开的** |
| **P-33** 力控轴阻尼相对世界 | 物体一动就把接触力扣光，反向推的板直接飞走 | 前馈 `kd·m·v_object`，等价于阻尼相对物体 |
| **P-36** 摩擦 buffer 有自己的下标 ⭐ | 一半环境摩擦翻倍、一半全零，stick/slide 标签整体错 | 摩擦按**它自己的** counts 取，再按法向力占比摊到接触点 |
| **P-37** 接触法向的正负约定 | 换个场景就翻，"用正面还是背面"判反 | 只用 `\|n_z\|` 判面/棱；哪一面用几何量且整条 episode 判一次 |
| **P-38** 运行时写关节阻尼 | 盖过资产里的值，改了资产等于没改 | **"结果逐位相同"= 改动没生效**，先查有没有被运行时覆盖 |
| **P-39** 力控轴掩码是世界轴对齐的 | 法向不与世界轴对齐时三轴全力控，切向指令失效 | 用 `FloatingPD(force_dir=...)` 做沿任意方向的混合控制 |
| **P-40** 阶段切换处目标跳变 ⭐ | 工具被甩出一米，且**只在某些家族上**发生 | 任何阶段切换处目标位置必须连续；**失败率按参数劈成两半时，那个分布就是线索** |
| **P-41** "力在区间内"是逐接触体判的 | 直擦两块板各分一半力，低于下限，一格都擦不掉 | 指令侧要按同一个粒度给 |
| **P-42** 伺服转动物体时参考系挂错 ⭐ | 力大半变成顶向圆心的无用功，PD 饱和而物体不动 | "提前量"和"贴合偏移"必须挂在**同一个**参考系上——接触体的实际位置 |
| **P-43** 单向关节却随机化转向 | **改什么控制都不动**：一半样本一上来就顶在死挡上 | "结果对控制不敏感"就去查约束（限位、行程），不要继续调控制 |
| **P-44** 光滑曲面上用纯力控 | 板顺着边沿滑下去钻到物体底下，1229 N 而力矩为零 | 力控只在接触面能提供**侧向约束**时才安全，否则用位置干涉 |
| **P-45** 力控从"零穿透"起步 | 板自由加速穿过被推的柱体；**接触点数全程为 0**（板与板的接触不在过滤名单里） | 先用位置干涉压出实接触，再交给力控 |
| **P-46** 板的长边没有与被推圆柱的轴平行 | 动态下板从圆柱面滑脱，而**静态位控下一切正常** | 怀疑几何摆错时先用静态位控测一遍：静态正常=要查动态接触条件 |
| **P-47** 把"任务已完成之后"算进脱手率 | 窗口留得越宽裕，数据看起来越差 | 指标只统计驱动段；判据本身不放宽，保持段的接触率单列 |
| **P-48** 板绕法向的滚转没人管 | 两块板的朝向标记互相反 180°，录像读不出姿态（物理上恒等，不影响结论） | 用 `quat_face_and_up` 把滚转写成显式参数，别让它从叉乘里掉出来 |
| **P-49** 闭环里每步重算的目标含离散选择 ⭐ | 目标四元数在两个**等价解**之间随机横跳，夹持力腰斩、物体被甩出一米 | 离散选择（符号/分支）必须在回路**外面**算一次；点积判正负前先问它会不会正好是零 |
| **P-50** 只录 env 0 而家族分配周期与 envs 同步 | 45 个批次只录到一个家族；拷产物时通配符又把服务器上的旧文件带了回来 | 家族按 `(b + i) % len(fams)` 轮换；产物**先清空再生成再逐个显式拷**，最后查文件数与内容重复 |
| **P-51** 夹持三个动作同时给 ⭐ | 直上直下落 -> 工具被楔飞；先张开落到位再夹 -> 压力归零（位置误差被消掉） | 顺序必须是**张开落下 → 夹拢 → 再下压**；位置控制里"压力"就是位置误差的产物 |

### 7.2 会让你卡住的

| 坑 | 现象 | 解法 |
|---|---|---|
| **P-19** headless 渲染 | `sim.step(render=True)` **静默卡死**（0% CPU、显存已占） | 一律 `render=False`；要出图单独调 `sim.render()` |
| **P-19** 关闭挂起 | `SimulationApp.close()` 挂起，进程变僵尸占显存 | 脚本末尾用 `os._exit()` |
| **规则 11** 仿真上下文 | 一个进程内第二次创建 `SimulationContext` 即挂起 | 一个进程只跑一项，用 shell 脚本串联 |
| **P-20** `pkill -f` | 匹配到自己的 shell，把自己杀了，后续命令全不执行 | 用 `pgrep -f "[x]xx"` 字符类；或拆成两条 ssh |
| **P-24** 相机 | `SimulationApp({"enable_cameras":True})` 无效 | 必须用 `AppLauncher(headless=True, enable_cameras=True)` |
| **P-24** 相机位姿 | `cam.set_world_poses_from_view()` 卡死 | 在 `CameraCfg.OffsetCfg` 里静态给定 |

### 7.3 接触力可能爆到几千牛（P-27）

`max_depenetration_velocity` 默认 5.0，求解器会用高速把穿透的物体推开，
产生巨大冲量。S2 实测峰值 **4384 N**（均值只有 13 N），把奖励曲线炸得
在 ±250 之间乱甩，239 轮训练作废。

两层都要修：惩罚封顶 + `max_depenetration_velocity=1.0`。
只封顶等于掩盖——尖峰还在，只是不体现在奖励里。

**并且：任何新任务环境的 `_get_rewards` 必须把每一项写进 `extras["log"]`。**
没有分项记录，训练不收敛时只能猜；我猜了三轮全错。

### 7.4 reward 设计的四个洞（D-31）

每个新任务都会遇到，**开训前跑 `tools/s2_scripted.py` 就能提前发现**：

1. 塑形项可以靠悬停刷分 → 用势函数式（只奖励差距的减少量）
2. 量纲失衡，最优解是"不动" → 在成功轨迹上标定权重，不要拍
3. **提前终止可以自杀刷分** → 失败终止必须有显式惩罚
4. `sign()` 造成奖励悬崖 → 改成差距的势函数式差分

---

## 8. 绝对不能做的事

这三件都会让数字变好看，也都会让结论作废。**任何一件发生了必须记进 `decisions.md`，不得静默处理。**

1. **留出任务不通过后，把它加进训练**（P-13）——数字立刻好看，主张立刻归零
2. **为了制造条件间差距而调任务参数**（P-11）——那是"调到 baseline 失败为止"
3. **跨形态结果里对某个执行器单独改写 envelope**（P-15）——四宫格必须来自同一个文件，且标 hash

还有一条 D-30 的硬规则：

> **在没有为某个组合训练过 Expert 之前，绝不允许把该组合的失败归因于"表示信息不够"。**

---

## 9. 环境版本（已锁定，不要升级）

| 项 | 版本 |
|---|---|
| Isaac Sim | `5.1.0-rc.19`（**是 RC 不是正式版**） |
| Isaac Lab | `2.3.1` = upstream `v2.3.1 (5c2ec81)` + 一处 `assets.py` 补丁；快照 commit `2ab57ade` |
| Python | 3.11.13（`/isaac-sim/python.sh`） |
| PyTorch | 2.7.0+cu128 |
| RL | rsl-rl-lib 3.0.1 |

**冻结时点**（D-21）：S2 开始训练后环境冻结。此前允许修复，此后任何变更
必须新增 D-xx 记录并说明已产出的 checkpoint 是否作废。

不升级 Isaac Lab 的理由见 D-20——upstream 新版的摩擦力接口是**聚合求和**的，
把空间分布抹掉了，而 `plan/02` §3.2 要的正是逐接触点数据。

复查 Isaac Lab 有没有被人改过：

```bash
ssh root@10.0.6.98 'cd /workspace/isaaclab && git status --short'   # 有输出=被改过
```

---

## 10. 工作方式（这个项目踩出来的）

1. **动手前先查 `decisions.md`**，看这个方案是不是已经被否决过。
2. **跑 RL 之前先跑脚本验证。** 三分钟省几个 GPU 小时，而且训练失败时能分清
   是"奖励配错了"还是"任务本来就做不到"。
3. **数字对了不等于对。** S1 出现过多次假 PASS（推子被卡住却报"推不动"）。
   **录像看一眼就能发现，数字发现不了。** `plan/06` §7 因此把人工看视频列为必经步骤。
4. **引用任何数字前先问它对参数敏不敏感。** 扫一遍成本很低（P-26 就是这么发现的）。
5. **踩到坑当场记 `pitfalls.md`**，改设计当场记 `decisions.md`，包括被否决的方案。
